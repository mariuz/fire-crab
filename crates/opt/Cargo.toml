[package]
name = "fire-crab-opt"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The optimizer's access-path selection: the conversion of Firebird's src/jrd/optimizer/, differentially tested against the engine's own PLAN output"

[dependencies]
fire-crab-ods = { path = "../ods" }

[[bin]]
name = "fcopt"
path = "src/main.rs"
