# Subsystem map: C++ engine → paper document → Rust conversion

The conversion's chart. Each row links the C++ source directory, the
companion-paper document that explains it (the recommended reading *before*
the source), the planned crate, and the status. Order within phases is the
planned conversion order; the criterion is always "what can be
differential-tested next with the least new machinery" (see
[methodology.md](methodology.md)).

Paper document links are relative to
[conceptual-architecture-for-firebird-paper](https://github.com/mariuz/conceptual-architecture-for-firebird-paper).

**Where the map stands.** Every row now carries a converted first slice with a
live differential against the C++ engine — the last "planned" row, platform
I/O, closed with `fire-crab-pio`. That is a statement about BREADTH, not
completeness: what remains is depth inside each row (the frontier paragraphs
say what, per row), and depth is where the hard parts live. A first slice
means the subsystem's laws are stated and checked, not that the subsystem is
finished.

## Phase 1 — storage, bottom-up (in progress)

| C++ source | Paper document | Crate / module | Status |
|---|---|---|---|
| `src/jrd/ods.h` (pag, header_page, tx_inv_page) | on-disk-structure.md | `fire-crab-ods` {pages, header, tip} | **done** — differential vs gstat, 123 dbs |
| `src/jrd/sqz.cpp` (record RLE) | on-disk-structure.md § records | `fire-crab-ods::sqz` | **done** — round-trip incl. FB4 forms |
| `src/jrd/ods.h` (page_inv_page / PIP) | on-disk-structure.md | `fire-crab-ods::pip` | **done** — bitmap + capacity formula tested |
| `src/jrd/ods.h` (pointer_page, data_page, rhd/rhde + flags) | on-disk-structure.md, transactions-and-concurrency.md | `fire-crab-ods::{pointer,data}` | **done** — record walk diffs vs live SELECT COUNT(*) (qa/diff-select.sh), 0 to 200k rows OK |
| record field decode via RDB$FORMATS | on-disk-structure.md, metadata-cache.md, catalog-bootstrap.md | `fire-crab-ods::format` | **done** — descriptors, null bitmap, dtype decode, blob-id resolution + level 0/1 segmented blob assembly, hardcoded system format for the bootstrap; full-row differential vs live SELECT (qa/diff-rows.sh) |
| `src/jrd/btr.cpp`, `btn.h` / index_root_page + btree_page + node encoding | indexing-and-full-text-search.md | `fire-crab-ods::btr` | **done** — leaf-level walk with prefix decompression; index-order differential vs live ORDER BY (qa/diff-index.sh), identical order at 200k rows through a multi-level tree |
| `src/jrd/blb.cpp` / blob_page | blob-handling.md | `fire-crab-blb` | **first slice converted — blob storage in BOTH differential directions, all three address levels**: `blh` headers and `blp` blob pages as typed codecs with every ods.h static_assert mirrored as a unit test; reading levels 0 (inline), 1 (page vector) and 2 (pointer pages) — a genuine 36 MB engine-created level-2 blob reads byte-for-byte against the engine's own OCTET_LENGTH/SUBSTRING answers; creation of levels 0 and 1 through fire-crab's own placement (`ods::insert_blob_slot` + `allocate_page`) with the referencing record's `bid` laid into the image — the engine reads every fcblb-written blob back in full, gfix -v -full -n finds nothing, gbak backs it up. THREE probe-settled laws: `blh_max_sequence` is the LAST sequence not the count (ods.h's comment misleads — blb.cpp:2377's `>` test decides; the engine read one page past a count-valued field and called the file corrupt), `blh_length` counts payload with framing excluded, and rhd_blob/rhd_stream_blob are 16/32 (a first-guess 8 made every blob "not a blob" — grep the header, never recall). Segment framing round-trips including zero-length segments; stream blobs raw. Slice 2 added LEVEL-2 CREATION (pointer-page layout probed off the engine's own 36 MB blob: blob-wide lead, sequence 0 on pointer pages, blp_length = entry bytes; an 18 MB fcblb-written level 2 reads back in full) and WIRE ADOPTION - the server's seven blob call sites now read through fire_crab_blb::read_blob_content, so op_open_blob/op_get_segment serve level-2 blobs, proven by node fetching every fcblb-written blob over the wire. Slice 3: the BLOB GC differential (the engine's own gfix -sweep collects fcblb-written blobs when their rows die — survivors intact through both readers, PIP free BITS rising by exactly the dead pages; the bitmap is the allocation truth, pip_used is not) and the BLOB CRASH WORKLOAD (fccch: nine blob pages riding AHEAD of the data pages naming them — the blob-before-data edge under real weight, 15/15 careful prefixes valid). Slice 4 added STREAM-BLOB creation (rhd_stream_blob, raw unframed content, count 1 and max_segment the whole length) with a ONE-WAY differential named as such: stream blobs come from the API's BPB and never from SQL (probed — an SQL literal blob stores SEGMENTED), so the engine cannot write one through isql; the gate holds the direction that exists — fire-crab writes at levels 0 and 1, the engine reads every byte back. Companion doc: [blob-conversion.md](blob-conversion.md) (qa/blb-levels.sh 20 + qa/blb-gc.sh 5 checks) |

## Phase 2 — the transaction system

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| `src/jrd/tra.cpp` TIP chain, `vio.cpp` version rules, `sqz.cpp` deltas | transactions-and-concurrency.md | `fire-crab-ods::tra` | **done** — TIP-chain state lookup, delta back-version reconstruction (Difference::apply), committed-only MVCC visibility walk; differential vs live SELECT on a file frozen mid-uncommitted-work (qa/diff-mvcc.sh) |
| `src/jrd/vio.cpp` GC/sweep (VIO_chase_record_version, cannotGC vio.cpp:1663) | garbage-collection-and-sweep.md | `fire-crab-ods::gc` | **done** — classifies collectable versions (expunge path + back-chain path) against the oldest-snapshot threshold; prediction differential vs live `gfix -sweep` (qa/diff-sweep.sh), predicted removal == actual removal, 210 versions across both paths |
| `src/lock/lock.cpp` | lock-manager.md | `fire-crab-lck` | **first slice converted — the lock table's policy, held against TWO oracles**: the compatibility matrix transcribed from `lock.cpp:150` and PINNED AGAINST THE ENGINE'S OWN SOURCE (`fclck pin-source` re-parses the vendored initializer and diffs all 49 cells), and the grant/deny behavior held against the LIVE server — `SET TRANSACTION RESERVING` maps reservation modes onto LCK_SR/SW/PR/PW, so a fifo-held mode probed by four `NO WAIT` reservations gives a 16-cell engine-arbitrated matrix that must equal `fclck compat` cell-for-cell (including the famous one: SR beside PW is COMPATIBLE — a protected writer tolerates MVCC readers). Converted with the engine's shapes: `lbl_counts` + the single-aggregate `lbl_state` grant probe (lock.cpp:2228 — sound because granted sets are mutually compatible), enqueue with WAIT/NO WAIT, convert tested against everyone-else's aggregate (lock.cpp:2535), dequeue + the FIFO `post_pending` regrant (late compatible arrivals refused behind a blocked head — no starvation), and the deadlock scan (wait-for DFS; the scanning request is the victim) — proven live by a two-attachment cross-update drawing the engine's own SQLSTATE 40001 deadlock. Slice 2 added OWNER TEARDOWN (purge_owner — live-verified: B's parked WAIT reservation proceeds the moment A detaches without committing), LOCK DATA WORDS (write requires holding, anyone reads, absent reads zero — a live LCK_tra lock on this box carries Data: 134) and the BLOCKING SET (who would receive the AST); plus a SuperServer finding: relation locks never appear in the shared lock table, so the structural dump differential is bounded to the cross-process series and reservations stay the relation oracle. Slice 3: KNOCK EVENTS (a parked request posts deduplicated knocks to its blockers; take_knocks drains; the downgrade protocol unit-pinned) and LOCK TIMEOUTS (deadline-carrying waits + expire(now); live: SET TRANSACTION WAIT LOCK TIMEOUT 2 draws the engine's lock time-out while the blocker holds). Companion doc: [lock-manager-conversion.md](lock-manager-conversion.md) (qa/lck-reserving-matrix.sh: 5 phases) **Slice 4 converts the SHARED TABLE**: the file the server maps (/tmp/firebird/fb_lock_*) decoded from lock_proto.h and printed in `fb_lock_print`'s own text - header counters, the hash-length distribution, the four queues and an owner block per owner, BYTE-IDENTICAL to the engine's tool on the same snapshot (both read a `cp` copy, because reading live shared memory twice gives two different tables). The layout's law: every pointer is a self-relative OFFSET (SRQ_PTR, que.h:108) since each process maps the region elsewhere, and a queue node sits INSIDE its block, so the tool prints `srq_forward - offsetof(own, own_lhb_owners)` - a raw 78408 prints as 78392. Two instructive bugs: three wrong counter offsets in the eleven-`FB_UINT64` run printed a plausible `0` on a quiet server and only the one reading into the hash table gave itself away; and a wrong `lhb_hash` offset produced a distribution that was NEARLY right (53 chains of length one where the engine counted 52). One number belongs to libc, not Firebird - `own_flags`'s offset depends on `pthread_mutex_t`/`pthread_cond_t` sizes, so a lock table is not portable across builds. Lock blocks, request details, the history ring and wait-for cycles stay unconverted. **SLICE 5 finishes the dump** - `-l` lock blocks with KEYS decoded per series (page locks print space:page with the STORED ORDER REVERSED, relation locks id:instance, transaction/attachment locks one 64-bit number, record-GC page:line, index-rescan rel:idx, otherwise a plain number, `<none>`, or printable bytes with `<NNN>` escapes), `-r` request blocks, and BOTH history rings (lhb_history titled History, shb_history titled Event log; operation 0 is an unused slot, skipped but still advancing the circular walk). Every switch byte-identical, `-a` at 1990 lines. Two more lessons of slice 4's shape: THREE queue-field offsets were shifted by one field and every check passed because Free requests, Blocks and Pending are EMPTY without contention (the gate now blocks a real waiter on a PROTECTED WRITE reservation; the wrong offsets differ by exactly eight bytes), and a series enum transcribed from a NUMBERED LISTING was off by one past LCK_attachment - visible on exactly ONE line in 636, series 22's 4-byte key which splits as 0004:0000. The engine's "AST: 0x%p" double prefix (glibc's %p already prints 0x) is reproduced, not corrected. Left: `-w` wait-for cycles, `lbl_counts`, and writing (qa/lck-table-dump.sh, 16 checks) |

## Phase 3 — cache and physical I/O

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| `src/jrd/cch.cpp` (page cache, latching) | page-cache-coherency.md, careful-writes-and-crash-safety.md | `fire-crab-cch` | **first slice converted — the careful-write precedence graph, held by a crash-matrix differential the engine itself judges**: the cache (`CCH_fetch`/`CCH_mark`/`CCH_precedence`/`write_buffer`'s recursive higher-queue drain/`clear_precedence`, cycles broken by writing the window page immediately — check_precedence's rule) sequences a real multi-page operation (a batch of `fire_crab_ods::dml` inserts growing the relation: header, TIP, PIP, pointer and data pages all change), and `fccch crash-matrix` materializes EVERY write prefix — the file had the process died after write k. `gfix -v -full -n` finds NO errors at any careful prefix and `isql` reads exactly the pre-operation rows (all committed, no phantom) until the TIP commit flip lands last; the NAIVE order (exact reverse) breaks at 5 of 8 prefixes — wrong-page-type corruption, short-file read failures, and PHANTOM ROWS from the uncommitted insert, the class careful writes exist to prevent. One rule was probed by the gate itself: the PIP bit goes out AFTER the data page but BEFORE the pointer (a pointer written ahead of its PIP validated with page errors; the reverse window is a benign orphan warning — leaked space, never harmed data). Ordered rules mirror the documented call sites: content before reference (data before pointer, btree before index root, blob before data), header's transaction high-water before every page naming one, the TIP flip last. Slice 2 taught the matrix INDEXES (B-tree maintenance joins the ensemble; the data-before-btree edge — an entry names a record NUMBER — made law after slice 1 passed it by page-number luck) and DELETES (header → stubs → tip; the naive order makes rows VANISH EARLY — the TIP-last law's other face), and caught a workload invalidity: an insert that skips index maintenance is not a valid operation on an indexed table. The BLOB workload (slice 3, with fire-crab-blb) put the blob-before-data edge under real weight: nine blob pages ahead of the records naming them, 15/15 prefixes valid (qa/cch-crash-harness.sh: 4 workloads × careful+teeth) |
| `src/jrd/pag.cpp`, `src/jrd/os/posix/unix.cpp` | on-disk-structure.md, careful-writes-and-crash-safety.md | `fire-crab-pio` | **first slice converted — the floor of the engine, checked against the engine's own accounting of it**. Very little code, and everything above it depends on all of it: `offset = page * page_size` (`seek_file`) — ABSOLUTE, with no rebasing, because Firebird 6 has NO multi-file databases (`jrd_file` is one descriptor, no chain, no `fil_min_page`; a converter carrying an older engine's file chain would place every page wrong); `PIO_get_number_of_pages` = `file_size / page_size` as INTEGER division (a trailing partial page is invisible, so a healthy file's length is a whole number of pages and checking that catches a truncation the division hides); `openFile`'s three flags, where FORCED WRITES turns out to be an OPEN MODE (`SYNC` in the open flags, decided from `hdr_force_write` at header offset 22) rather than an fsync policy — which is why changing it reopens the file and flushes FIRST when switching on; `PIO_extend`'s fallocate arithmetic; and `PIO_init_data`'s floor — it never writes below PAGE 8, which holds the header, the first PIP and the first pointer page. The LOCK law is the one with consequences for this whole project: `lockDatabaseFile` flocks the file (LOCK_EX when `getServerMode() == MODE_SUPER`) and reports a busy lock as `isc_already_opened` — *"Database already opened with engine instance"* — while fire-crab's readers take NO lock, which is precisely what lets every differential here read a database the server holds open (at the price of needing its own freshness signal). Differentials: the page count equals `MON$DATABASE.MON$PAGES` on four databases including two points of growth (313→401 pages for 12000 rows); every page whose type `RDB$PAGES` records is found at `page*page_size` with that type (84 of 84), while the same comparison shifted ONE page matches 0 of 84 — the check discriminates; a 20-byte header read already yields the page size (the reason `PIO_header` exists); `gfix -w sync|async` moves the header bit, our decode and `gstat -h`'s Attributes together, and with forced writes on a copy taken after COMMIT holds every row; the lock probe says BUSY while the engine is attached and FREE after, with a fire-crab read succeeding in both; and a page past the end is refused rather than answered with zeros (qa/pio-layout.sh, 18 checks). Frontier: shadowing, nbackup difference files, raw devices, real O_DIRECT, and the Windows half of the layer |

## Phase 4 — language: BLR, DSQL, the executor

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| BLR decode (`par.cpp` structure, `blp.h` + gds.cpp operand table) | blr-intermediate-language.md | `fire-crab-ods::blr` | **done** — operand-atom walker + verb table (171 verbs) converted from the engine's own printer; verb-token differential vs isql `SET BLOB ALL` (qa/diff-blr.sh), every decodable blob matches token-for-token, unknown verbs reported not guessed |
| `src/dsql/` (SQL → BLR) | grammar-and-parser.md, dsql docs | `fire-crab-dsql` | **forty-seven slices converted + differential-tested against FOUR of the engine's own oracles (seven catalog stores)** — the view-shaped SELECT compiles to BYTE-IDENTICAL BLR, verified against `RDB$VIEW_BLR` (the artifact the engine's DSQL itself stores for the same statement). Covered: WHERE booleans (AND/OR/NOT with the engine's De-Morgan folding, comparisons, IS NULL, BETWEEN, LIKE), VALUE EXPRESSIONS (add/subtract/multiply/divide/negate/concatenate — a sign before a numeric literal folds into it), IN lists (blr_in_list + u16 count; NOT IN keeps blr_not), MULTI-STREAM RSEs (comma-FROM and INNER JOIN ... ON as a nested blr_join), aliases (blr_relation2, the alias UPPERCASED IN DOUBLE QUOTES), qualified fields by context (bare fields in multi-stream refuse — catalog-free), OUTER JOINS (blr_join_type 1/2/3 after the streams; absent for INNER), JOIN CHAINS (left-nested: each join node holds the previous as its first stream slot), INT64 literals (blr_int64), and the first BUILT-IN FUNCTIONS: UPPER/LOWER, CHAR_LENGTH/OCTET_LENGTH (blr_strlen + length-type byte), SUBSTRING (0-based start compiled as an UNFOLDED subtract(from,1)), TRIM (where + spec bytes) — an unknown name before `(` refuses, never a field — and CAST (blr_cast + probed dsc layouts: NUMERIC(p≤4) short but DECIMAL(p≤9) always long) with the CONDITIONALS: searched CASE/IIF = ONE blr_cast over a blr_value_if chain typed by the probed unification law (max int-digits + min scale, dtype that fits — long⁰∪long⁻¹ widens to int64), simple CASE = blr_decode, COALESCE = blr_coalesce (both wrapper-free), NULLIF = cast(value_if(a=b, NULL, a)) typed from its branches only; field branches under a cast wrapper refuse — and the SUBQUERY PREDICATES: EXISTS = blr_any over one rse (subquery WHERE = its boolean, select list traceless), SINGULAR = blr_unique, IN (SELECT)/ANY/SOME/ALL = blr_ansi_any/_all with a double-nested rse (the outer rse's single stream IS the subquery's rse; the comparison is the outer rse's boolean); negation flips the quantifier and inverts the comparison (NOT IN = ansi_all+neq); subquery streams join the context numbering but stay invisible to outer bare names — plus DISTINCT (blr_project: the one place the select list leaves a trace), scalar subselects (blr_via(blr_singular(rse), value, null)), derived tables (an rse in the stream slot, alias text `"X" "PUBLIC"."T"`, ONE shared context), and UNION [ALL] (blr_union stream with per-branch rse + blr_map; distinct form adds blr_project over blr_fid) (qa/dsql-view-blr.sh, 127 checks incl. refusals). Slice 7 opened oracle number two — `RDB$PROCEDURE_BLR`: `compile_procedure` emits a whole `FOR SELECT ... DO SUSPEND` body byte-identically (message with dsc+null-flag pairs and the EOF short, declares with NULL-inits, stall, labels, blr_for whose stream is CONTEXT 0 — procedures number from 0, views from 1 — ORDER BY as blr_sort asc/desc after the boolean, twin sends via blr_parameter2), unlocking what views cannot hold — and AGGREGATES + GROUP BY: blr_aggregate as a STREAM with its own context over the source rse (WHERE stays the source's boolean), blr_group_by in clause order vs blr_map in select-list order (probed to differ), agg verbs 0x53–0x57/0x5D, HAVING as the outer rse's boolean over blr_fid slots (equal aggregates REUSE their map slot, fresh ones append), ORDER BY sorting fids — and INPUT PARAMETERS: message 0 (dsc + null-flag pairs, no EOF slot), the loop under blr_receive 0, `:name` compiling to blr_parameter2(0, 2i, 2i+1) straight as a value anywhere in the FOR select — plus the SINGULAR `SELECT INTO` (blr_for over blr_singular, no label 1, SUSPEND a sibling send), FIRST/SKIP (rse sub-clauses between streams and boolean), and the DISTINCT aggregate verbs (COUNT/SUM/AVG dedicated; MIN/MAX fold to plain) (qa/dsql-proc-blr.sh, 48 checks incl. refusals). Slice 11 opened oracle number three — `RDB$TRIGGER_BLR`: compile_trigger emits whole trigger bodies byte-identically (the leanest wrapper: begin, label 0, DOUBLE begin, statements, three ends; OLD=ctx 0, NEW=ctx 1 as pseudo-streams; assignments, IF with a bare blr_end for a missing ELSE, nested blocks as double begins; the header leaves no BLR trace) — and THE DML VERBS: INSERT = blr_store(relation, assignments, column list required), DELETE = blr_for over a marks(1,4)-stamped rse + blr_erase, UPDATE = the same loop + blr_modify(org, new) with the new-record context allocated BEFORE the rse stream's; SET targets write new, sources and WHERE read org — and PSQL CONTROL: DECLARE [VARIABLE] with initialisers (triggers group declares-then-inits; procedures interleave — both probed), local-variable reads/assignments (blr_variable), WHILE = label N + blr_loop + if(cond, body, blr_leave N) with encounter-ordered labels, INSERTING/UPDATING/DELETING = eql(blr_internal_info(6), 1/2/3) composing under NOT/AND/OR (qa/dsql-trig-blr.sh, 34 checks incl. refusals). Slice 14 UNIFIED the surfaces: procedure bodies run the same statement machine as triggers — outputs ARE variables (locals continue the numbering), SUSPEND is a statement, RETURNS is optional (one-slot EOF message), bare names resolve vars-then-inputs outside stream scopes but stay columns inside them, and bodies mix FOR SELECT / SELECT INTO / DML / IF / WHILE / SUSPEND freely. Slice 15 added the CALL surface: EXECUTE PROCEDURE = blr_exec_proc (counted name, u16 in-count+values, u16 out-count+variables), EXCEPTION = blr_abort+2+name, EXIT = blr_leave 0 (the wrapper's label); (FOR) SELECT works in trigger bodies (stream after OLD/NEW), and the aggregate-context law generalised to stream+1 at any context (qa/dsql-proc-blr.sh 67 + qa/dsql-trig-blr.sh 38 checks incl. refusals). Slice 16 opened oracle number four — column BLR: DEFAULT clauses (RDB$DEFAULT_VALUE) and COMPUTED BY expressions (RDB$COMPUTED_BLR), both `blr_version5 <value> blr_eoc` — defaults as narrow as the engine's grammar (literals/NULL/CURRENT_DATE-TIME-TIMESTAMP), computed columns taking the whole expression surface at an anonymous context 0. Slice 17 added domain defaults (RDB$FIELDS), expression indexes (RDB$INDICES.RDB$EXPRESSION_BLR — the computed-column frame again) and CHECK constraints (the engine's own system triggers: blr_if over the NEGATED condition, blr_abort gds_code 'check_constraint', fields at ctx 1) — and closed a latent gap the CHECK battery exposed: non-integer IN-list items are CAST to the left side's catalog type, so they refuse now. Slice 18 added the seventh store — domain validation (RDB$VALIDATION_BLR: the RAW un-negated boolean with VALUE = blr_fid(0,0), a different shape from table CHECKs) — plus GEN_ID (blr_gen_id: name + increment) vs NEXT VALUE FOR (blr_gen_id2: name alone — two verbs for one concept, chosen by syntax) and POST_EVENT (blr_post + value). Slice 19 added error handling: WHEN-carrying blocks = blr_block (guarded begin, blr_error_handler + code — ANY/EXCEPTION-name/GDSCODE-name — handler statement, end) while plain blocks stay double-begins in both body kinds; ROW_COUNT = internal_info(5). Slice 20 converted UPDATE OR INSERT (a begin: marks-stamped modify-loop with blr_equiv on the MATCHING column, then if(row_count=0, store); contexts store/modify-new/rse-org in that order; MATCHING required and single-column), added CURRENT_CONNECTION/CURRENT_TRANSACTION (internal_info 1/2), and settled multiple handlers (sequential sections) and handler-body blocks (blr_block nested again) — flips ten and eleven. Slice 21 added autonomous transactions (blr_auto_trans), multi-column MATCHING (equivs left-nested under blr_and), WHEN SQLCODE (code 1 + i16 — flip twelve), and CURSORS (blr_dcl_cursor with the cursor name riding the relation2 alias, blr_derived_expr outputs, blr_cursor_stmt open/close/fetch) — plus a pinned ordering law: variable inits DEFER past following cursor declarations. Slice 22 COMPLETED the cursor surface: WHEN SQLSTATE (code 8 + counted string — flip thirteen), handlers carrying their OWN handlers (the nested block keeps its error-handler section — flip fourteen), cursor table ALIASES (the relation2 alias string becomes `"CX" "A"` — cursor name + table alias — displacing the schema-qualified table) with qualified columns resolving through the one stream, AGGREGATE cursors (blr_aggregate nested at ctx+1, claiming a SECOND context slot; outputs and fetch-sources are BARE blr_fid slots — no derived_expr wrapper; the engine demands AS names), and POSITIONED DML — DELETE ... WHERE CURRENT OF = blr_erase at the CURSOR's own context, UPDATE = blr_modify from it to ONE fresh slot, both stamped blr_marks(1, 1) TRAILING the verb where a DML loop's marks(1, 4) LEAD its rse; the INTO-less FETCH that positions them carries an empty begin/end. Slice 23 added MERGE — one probed sentence: `for(marks(1, 6), rse(join2(source@0, target@1, [LEFT], ON), [branch-union boolean]), if(missing(dbkey(target)), store, modify/erase + marks(1, 2)))` — the join LEFT only when a NOT MATCHED branch needs unmatched rows (matched-only merges are INNER with NO rse boolean), the boolean ORing branch conditions in canonical order (SQL branch order leaves no trace), branch contexts allocating UPDATE's new record then INSERT's store, the store re-emitting the aliased target — plus FOR SELECT ... AS CURSOR (the labeled FOR loop whose relation2 alias carries the cursor name exactly like a DECLAREd cursor's; INTO optional, its sources derived_expr-wrapped; WHERE CURRENT OF at the FOR's own context, body-scoped) and cursors in TRIGGER bodies (declarations keep their source slot among the grouped declares — the trigger flavor of the deferral law — numbering past OLD/NEW). Slice 24 added CONDITIONAL MERGE branches (WHEN [NOT] MATCHED AND <cond> joins the rse boolean's branch term — and(not(missing), cond) / and(missing, cond), the bare cond for matched-only — and nests if(cond, action, bare end) in the branch's slot), SCROLL cursors with the FETCH directions (blr_scrollable before the dcl_cursor rse; FETCH <dir> FROM = cursor_stmt sub-verb 3 + direction byte 0–5 + an offset value, blr_null unless ABSOLUTE/RELATIVE; only NEXT is legal unscrolled), DML RETURNING ... INTO — THREE shapes for one clause: INSERT = blr_store2 with a second returning begin, UPDATE = blr_modify2 under a SINGULAR rse with returning reading the NEW record, DELETE = no erase2 at all but a begin(returning-assigns, plain erase) under a singular rse — and EXECUTE STATEMENT (plain = blr_exec_sql + the literal; [FOR] ... INTO = blr_exec_into, u16 out-count, the sql, flag 1 singleton / flag 0 + the labeled DO statement, the variables LAST). Slice 25 added MULTI-BRANCH MERGE — branches of one kind form an if-else chain in SQL order, each conditional branch if(cond, action, <next>) with the next if filling the else slot BY POSITION, the last conditional getting a bare end, an unconditional LAST branch filling the else directly; the rse boolean ORs kind-terms built from left-nested or-chains (and(not(missing), or(c1, c2, ...)) / and(missing, or-chain)), each simplified to its bare missing-test when the kind has an unconditional branch; contexts allocate BY KIND in branch order (every matched UPDATE claims a new-record slot, then every INSERT its store) — PARAMETERIZED EXECUTE STATEMENT (('sql') (vals) = the FULL blr_exec_stmt with tag-prefixed clauses in fixed order: 1 in-count, 2 out-count, 3 sql, 4 the FOR form's DO statement, 11 input values, 13 output variables, blr_end; the literal no-param forms keep their compact verbs) and WITH LOCK (blr_writelock between the stream and the boolean — in FOR SELECT, DECLAREd cursors and AS CURSOR loops alike). Slice 26 added SUBROUTINES: DECLARE FUNCTION/PROCEDURE = blr_subfunc_decl/blr_subproc_decl — counted name, type 0, a flag byte (deterministic for functions, SELECTABLE for procedures), u16-counted parameter-name lists (the function's return slot an UNNAMED output), then the WHOLE inner body's BLR as a u32-counted blob, compiled by the same body machinery on a fresh parser; RETURN <expr> = begin(assign the unnamed slot 0, the no-EOF send, blr_leave 0); calls are blr_invoke_function value expressions / blr_invoke_procedure statements with the sub id clause (4 sub, 3 counted name) and u16-counted args; a subroutine's INPUTS reserve variable slots (locals number past them — top-level bodies don't) and a void subroutine drops blr_stall — plus NAMED EXECUTE STATEMENT parameters (tag 12: counted name + value each) and the ON EXTERNAL / AS USER / PASSWORD / ROLE modifiers (tags 5/6/7/14; any modifier forces the full blr_exec_stmt form) — AND A LATENT FIX: procedure LOCALS group (declare, declare, init, init) where outputs interleave; sixteen slices of single-local bodies read as interleaving until a two-local probe settled it. Slice 27 opened STREAMS INSIDE SUBROUTINES: blr_relation3 — counted schema PUBLIC, counted EMPTY package (the mystery byte, read from RelationSourceNode::genBlr), the name, then the alias slot ALWAYS present (relation2's alias string — quoted alias or cursor-name string — or a counted empty), the context; one emission switch turns the whole converted surface loose in subroutine bodies (INSERT, DML loops, singular SELECT INTO, FOR SELECT, DECLAREd cursors, AS CURSOR positioned DML, UPDATE OR INSERT, MERGE — battery-verified, most never probed in subroutine clothing), and zero-arg sub-function calls compile with the argument tag at count 0. Slice 28 flipped one of the oldest refusals — ALIASED STREAMS in body statements (FOR SELECT / DELETE / UPDATE with table aliases: relation2 + quoted alias at top level, relation3's always-present alias slot inside subroutines, qualified references resolving through the unchanged field machinery, aliased aggregate sources with qualified group keys included) — and let SUBROUTINES into TRIGGER bodies: DECLARE PROCEDURE/FUNCTION takes the same grouped-declare slot cursors do, calls working from trigger statements with outer-scope arguments like NEW.ID. Slice 29 dropped the second slice-7 refusal in two slices — SUBQUERIES IN BODY STATEMENTS: EXISTS/SINGULAR, IN (SELECT)/ANY/ALL and scalar subselects in body WHEREs and assignments, the whole view-compiler machinery carried over on TWO changes (the subquery stream takes the NEXT context id at the statement's numbering — the view formula was this law at base 1 — and a `host` slot keeps the enclosing statement's stream visible to qualified names, probed on an EXISTS correlated by table name); a scalar subselect in an assignment claims ctx 0 — plus ALIASED AS CURSOR (the cursor name pairs with the table alias, `"CU" "E"`, riding relation3's alias slot inside subroutines). Slice 30 added AGGREGATE SCALAR SUBSELECTS — via(singular, rse1(aggregate at the NEXT slot over the inner rse, its WHERE INSIDE, zero group keys, a one-slot map), fid(agg, 0), null), the aggregate claiming the slot after its stream as everywhere — in assignments, WHERE comparisons, SET values and subroutine bodies; subqueries inside a CURSOR's rse stay guarded with a peculiar probed law in hand (their streams INHERIT the cursor alias string — an EXISTS stream carried `"CX" "PUBLIC"."T"`). Slice 31 CONVERTED that infection: a post-parse STAMP walks the cursor's boolean tree pairing the cursor name with each subquery stream's own alias (`"CX" "X"`) or schema-qualified name — DECLARE CURSOR, AS CURSOR and cursor-in-subroutine (relation3's alias slot) all pinned — plus DISTINCT aggregate scalars (the dedicated COUNT/SUM/AVG verbs, MIN/MAX folding DISTINCT away — the slice-10 law reaching the scalar surface). Slice 32 added JOINS IN BODY FOR SELECTs — INNER/LEFT/RIGHT/FULL chains in FOR SELECT and the singular SELECT INTO, the view's left-nested chain at body numbering (ON at the join level, WHERE at the rse level, join_type absent for INNER), the MERGE scope generalized to a stream-index RANGE with qualified-only resolution (the gate itself caught a bare column binding to the FIRST stream where the engine resolves through the catalog — a wrong-bytes hazard closed before shipping) — and PACKAGED ROUTINE CALLS: EXECUTE PROCEDURE PKG.P = blr_exec_proc2 (counted package + name, u16 counts), PKG.F(x) = blr_function2 (a count BYTE) — compact verbs, not the invoke forms subroutines take. Slice 33 completed the join story: AGGREGATES OVER JOINS (the chain inside the aggregate's inner rse with the WHERE after it, the aggregate claiming the slot after ALL join streams — the slice-8 next-slot law counting past a chain; GROUP BY/HAVING/ORDER BY on the existing map machinery) and JOINED CURSORS (both streams carrying the cursor pairing — `"CX" "A"` / `"CX" "B"` — via the slice-31 stamp; each output's derived_expr wrap naming its column's OWN stream; positioned DML on joined cursors refusing — not updatable). Slice 34 added WINDOW FUNCTIONS: `blr_window` wraps the inner rse (its WHERE inside), then a window count and per window `blr_partition_by` — context, partition keys as source fields then REMAPPED as fids into the window's own map, a sort clause, the map; passthrough columns live in the DEFAULT (empty-spec) window beside empty-spec OVER () functions, each distinct (partition, order) spec gets its own window in ENCOUNTER order claiming the contexts after the stream, maps hold items in select order with partition keys appended, outputs read per-window fids; the aggregate verbs work windowed and ROW_NUMBER/RANK/DENSE_RANK ride `blr_agg_function` (counted name + an argument count); frame extents initially guarded. Slice 35 completed the window story: LAG/LEAD/FIRST_VALUE/LAST_VALUE under blr_agg_function with TRUE argument counts (LAG/LEAD canonicalize to three — value, offset filled with literal 1, default filled with blr_null), and FRAME EXTENTS on the v4 `blr_window_win` verb — subcoded clauses (1 partition carrying the v3 layout, 2 order, 3 map, 4 extent unit RANGE 0 / ROWS 1, 5 frame bounds, 6 frame values) with its OWN blr_end; UNBOUNDED is a bound sans value, a single bound implies CURRENT ROW as frame two, and v3/v4 windows mix freely in one statement. Slice 36 added NTH_VALUE (a FROM FIRST indicator — literal 0 — appended as the third agg_function argument) and DERIVED TABLES in body FOR SELECTs (the view convention at body numbering: the inner rse in the stream slot, inner WHERE inside, outer WHERE at the rse level, ONE shared context; pass-through lists only, derived-column aliases guarded) — and PINNED derived tables in subqueries, a shape LIVE through composition since the subquery slice without a single covering test. Slice 37 added DERIVED-COLUMN ALIASES: the derived list records (outer, inner) name pairs and every outer reference — qualified or bare, in items, WHEREs, ORDER keys or correlated subqueries — TRANSLATES to the inner column at the shared context (`D.X` emits the underlying `UID`); one translation point in the field resolver, the hardcoded bare-name sites routed through it. UNION in body FOR SELECTs probed (blr_union claiming the statement's first slot, branch streams following, fid outputs) — a named refusal with its transcript; named windows guarded as an engine-side probe blocker. Slice 38 cashed the union transcript — UNION [ALL] in body FOR SELECTs: `blr_union` claims the statement's FIRST slot (a lookahead reserves it before the branch streams number), per-branch rses with their WHEREs inside and positional maps, DISTINCT appending `blr_project` over the union fids, INTO reading them — and flipped a TWENTY-SIX-slice-old refusal: INSERT..SELECT, a marks(1, 4)-stamped FOR loop over the source rse storing one row per source row, the SOURCE stream numbering first, working in procedures, subroutines and triggers. Slice 39 added WITH CTES — the engine INLINES a non-recursive cte as a derived table (the cte name becoming the relation2 alias, column aliases translating, inner WHERE inside); the parser records the body's token SPAN at WITH and expands it at the FROM reference, one cte referenced exactly once — and EXPRESSION INSERT..SELECT (source items as full value expressions via the two-phase list parse), the FIFTIETH refusal to fall; GROUP BY expressions probed and left named. Slice 40 cashed that transcript and two more flips: SELECT-ITEM EXPRESSIONS (full value surface at the stream context, bare names resolving as columns), GROUP BY EXPRESSIONS (the group list takes the raw expression, the map its BARE fields in select-item order — each item contributing its deduped fields or its aggregate verb, the slice-8 law generalized — items AND aggregate ORDER keys rebuilt over the fids; the gate itself caught ORDER BY SALARY / 1000 refusing) and MULTIPLE CTES (comma-separated WITH lists, each expanding once, pinned inside a correlated subquery). Slice 41 opened five doors: EXPRESSION SORT KEYS (raw expressions in the sort clause; bare literals refuse — the engine reads them as POSITIONS), WINDOW PASSTHROUGH EXPRESSIONS (fields into the default window's map, items rebuilt over fids), OFFSET/FETCH (the standard spelling of SKIP and FIRST, same clauses in the same order; PLAN/NATURAL/OFFSET/ROWS/ONLY joined the keyword list after PLAN was slurped as a table alias), PLAN (tbl NATURAL) (`blr_plan` + `blr_retrieve` + the stream re-emitted + `blr_sequential`, last in the rse) and PARAMETERS IN HAVING — a slice-9 refusal, thirty-two slices old, felled by the gate's own battery statement; HAVING without GROUP BY pinned as an open door. Slice 42 added DISTINCT IN BODY FOR SELECTs (blr_project over the select columns after the boolean - the view law at body numbering), PLAN (tbl INDEX (names)) (blr_indices + counted names in place of blr_sequential) and ROWS m TO n (the legacy limits desugaring to UNFOLDED arithmetic - first = add(subtract(n, m), 1), skip = subtract(m, 1)); recursive CTEs probed (blr_recurse - the recursive branch STREAM-LESS, reading the recursion context by fid) and left a named refusal with the transcript. Slice 43 cleared the EXECUTOR's frontier list - five flips the exe gate had named: STARTING WITH, FIRST/SKIP (:param) (parenthesized only - FIRST :P is a syntax error in the ENGINE too), DISTINCT over derived tables, FOR-less SUBSTRING (length filled with INT MAX), and expression items in quantified subqueries (blr_derived_expr over the subquery stream); NULLIF/IIF field branches are a MODEL BOUNDARY (the unifying cast needs catalog types). Slice 44 cashed the union-in-subquery transcript (the union claims the reserved subquery slot, branches follow, the comparison reads fid(union ctx, 0)); the gate's own battery then found the NEXT law — expression branch items UNIFY the union's type (cast(int64) wrapping plain branches when a multiply promotes — the CASE-unification law in union clothing, named with transcript). Slice 45 cashed the RECURSIVE CTE transcript (flip 68): blr_recurse with the SECONDARY recursive-context byte, the anchor rse + map (cte name on the relation2 alias), the STREAM-LESS recursive branch reading fid(recurse ctx, 0), no terminator of its own — and the unification law's third appearance: a + 1 recursive item types int64 CATALOG-FREE (dialect-3 ADD is width-independent) so the anchor's field wraps in cast(int64), while multiplication refuses in RECURSION (rank-dependent). Slice 46 CONVERTED the unification law for subquery unions (one +/-/* literal branch types the union int64 catalog-free, plain branches wrap cast(int64) — the slice-44 gate-found refusal flipped) and added PLAN (tbl ORDER idx) (blr_navigational + one counted name, no count byte; the engine demands the matching ORDER BY). Slice 47 added MULTI-COLUMN recursive ctes and refined the unification law to PER COLUMN — in a two-column recursion where one column steps and the other rides plain, only the stepping column's anchor item wraps in cast(int64), both inside one map (qa/dsql-proc-blr.sh 278 + qa/dsql-field-blr.sh 39 + qa/dsql-trig-blr.sh 48 checks incl. refusals). The interpreted SQL surface inside `fire-crab-wire::server` (expressions, functions, CASE, predicates, DML, DDL, PSQL) remains its own path, documented source-by-source in [expression-surface.md](expression-surface.md); the two meet as this crate grows toward full statement compilation |
| `src/jrd/exe.cpp`, rse execution | query-optimizer-and-execution.md, request-lifecycle-code-trace.md | `fire-crab-exe` | **first slice converted — the statement looper executes STORED procedure BLR against the database file, differentially tested against the engine running the same bytes**: `fcexe` reads `RDB$PROCEDURE_BLR` (the blob `fire-crab-dsql` already matches byte-for-byte from the SQL text), parses the `FOR SELECT ... DO SUSPEND` wrapper (messages, interleaved declares+inits, stall, labels, the for-loop, twin sends) and runs it through the leanest Volcano tower — FullTableScan (the committed-visibility scan already converted as `fire_crab_ods::visible_rows`) under FilteredStream (the boolean with the engine's three-valued logic) under SortedStream (blr_sort keys; NULLs sort LOW — first ascending, last descending, probed). `SELECT * FROM <proc>` on the C++ engine must answer the identical rows: SQL → (fcdsql, byte-checked) BLR → (fcexe) rows, every arrow held against the original. Slice 2 flipped three of its own refusals and grew the tower: INPUT PARAMETERS (message 0 bound up front, blr_receive transparent to the synchronous looper, blr_parameter2 as a value expression — fcexe takes CLI arguments typed by the message slots), AGGREGATES (blr_aggregate as a stream with its own context: group-by keys fold rows NULL-grouping-with-NULL, the map's verbs — count/count2/total/average/min/max — with the engine's empty-set semantics: COUNT 0 but SUM/AVG/MIN/MAX NULL, ONE row for a keyless aggregate of nothing, integer AVG truncating; outputs read blr_fid, HAVING is the outer boolean over the aggregate frame), the SINGULAR select (a second row is a runtime error, not a truncation) and FIRST/SKIP (rse clauses, applied sort-skip-first). Slice 3 flipped three more: VALUE EXPRESSIONS (blr_add/subtract/multiply/divide/negate/concatenate — NULL propagates, integer division truncates, divide-by-zero and overflow are runtime ERRORS), INNER JOINS (blr_rs_stream = the NestedLoopJoin: aliased relation2 streams, the ON boolean over the joined frames; the executor generalized from single frames to BINDINGS — one frame per joined stream — so booleans, sorts, DISTINCT and aggregates compose over joins for free) and DISTINCT (blr_project: sort-based unique on the projected values, NULL grouping with NULL — the engine's null-first order held unprompted). Slice 4 flipped two more: OUTER JOINS (blr_join_type 1/2/3 — LEFT/RIGHT/FULL over two streams; a preserved row with no ON match emits once with the other side's frame an EMPTY row, every field of which reads NULL — the engine's null-padded record for free from the binding model; WHERE ... IS NULL over the padded side works, the anti-join) and DERIVED TABLES (a nested rse standing in the stream slot — its bindings pass straight through, the inner context IS what outer references name; one arm of the parser, zero executor code). Slice 5 flipped two more: JOIN CHAINS (rs_stream nests as a SOURCE of the outer rs_stream — the left-nested shape dsql emits; join sources went recursive, so a nested join's bindings splice whole into the outer product, and a LEFT join over a chain pads every context the nested side binds) and INDEX RETRIEVALS (PLAN (T INDEX (...)) executes as the real BitmapTableScan: the B-tree walk — ods::walk_index_leaves — yields record numbers, records fetch by number, and VISIBILITY IS DECIDED ON THE RECORD ITSELF, the index only saying where records might be; the index name resolves through RDB\$INDICES to its 0-based root slot; PLAN NATURAL runs the plain scan). Slice 6 flipped UNION (blr_union in the stream slot — its own context, per-branch rse + positional map, branches concatenating in order; the byte is 76, blr_eoc's, disambiguated purely by POSITION, and a union has NO terminator of its own — the branch count bounds it, the next end belongs to the OUTER rse, a law the first parse consumed one end too many to learn; the DISTINCT form rides the existing blr_project over the union's fids) and the remaining simple booleans: IN-list (u16-counted, 3VL — no match beside a NULL comparand is UNKNOWN), BETWEEN, LIKE (character-based backtracking, the porting playbook's own pseudocode) and STARTING WITH. Slice 7 flipped SUBQUERY PREDICATES and SCALAR SUBSELECTS: EXISTS = blr_any over one rse (correlation is FREE — the outer frames are already on the stack when the inner scan evaluates), SINGULAR = blr_unique, IN (SELECT)/quantified = blr_ansi_any/_all over a wrapper rse whose single STREAM is the subquery (the derived-table arm again) and whose boolean is the comparison — ANY: true beats unknown beats false, ALL the mirror, the empty set false/true; NOT IN's 3VL trap holds (a NULL in the subquery poisons every row to UNKNOWN — the gate's ID NOT IN (SELECT AMT...) answers empty exactly as the engine does); scalar subselects = blr_via(singular-rse, value, null) with sing_err on a second row; blr_derived_expr is a bookkeeping wrapper that parses through. Slice 8 felled the LAST named refusal — WINDOWS (blr_window): every source row survives (a window is an aggregate that KEEPS its rows); per window (blr_partition_by): whole-partition aggregates when the window has no ORDER, RUNNING aggregates over the default RANGE frame when it does — peers included, rows with equal sort keys share the value — and ROW_NUMBER/RANK/DENSE_RANK (blr_agg_function) by peer-group position; the remap fids after the partition keys are bookkeeping and parse away; windows process in declaration order, each re-sorting the row set, the LAST window's order being the emission order (the engine's sort-per-window pipeline, multi-window check held). Slice 9 added the CONDITIONAL VALUE VERBS — CAST (blr_cast + dsc: integer range checks ERROR rather than wrap, text width overflow is the engine's 22001 rather than a silent cut, NULL passes), COALESCE (first non-NULL), and blr_value_if (searched CASE under its unifying cast; UNKNOWN takes the else branch) — and the VALUED WINDOW FUNCTIONS: LAG/LEAD (offset rows in partition order, the third argument when they run out), FIRST_VALUE, LAST_VALUE (the default RANGE frame's famous trap: the CURRENT PEER GROUP's last value, not the partition's — engine-verified) and NTH_VALUE (the nth only if the frame reaches it). Slice 10 flipped FRAME EXTENTS — the v4 blr_window_win form (subcoded partition/order/map, extent unit RANGE 0 / ROWS 1, frame bounds 0-prec/1-foll/2-current with optional values, its OWN blr_end; a single bound implies CURRENT ROW): ROWS frames offset by row position (sliding sums, lookahead windows, CURRENT-to-UNBOUNDED tails, framed LAST_VALUE all engine-verified), RANGE frames in their value-less peer forms; Slice 11 flipped both: RANGE VALUE BOUNDS (key arithmetic over the single sort key — the frame is the contiguous run of rows whose key lies within cur∓v on the traversal's own axis, DESC flipping the sign, NULL keys framing their peer group alone and never qualifying for edges), CROSS-FAMILY CASTS (int→text renders plain decimal, text→int parses with the engine's conversion error on a bad string — width and range errors as before, never silent), and the SIMPLE CASE (blr_decode: operand, counted condition values, counted results with the extra one as ELSE; a NULL operand matches nothing). Slice 12 flipped the STRING FUNCTIONS: UPPER/LOWER (blr_upcase/lowcase), CHAR_LENGTH/OCTET_LENGTH (blr_strlen + the length-type byte), SUBSTRING (blr_substring — the 0-BASED start arriving as the reference compiler's unfolded subtract, negative length/start runtime errors, past-the-end empty) and TRIM (where byte: both/leading/trailing over spaces; trim-by-character refuses). GEN_ID (a SELECT that WRITES — fcexe is read-only) and TRIM-by-character are named. Slice 13 was THE WIRE ADOPTION: fcwire serves SELECT FROM <proc> BLR-FIRST through this crate (the stored bytes fcdsql matches, executed; source-interpreter fallback outside the surface) — windows, frames, LAG, correlated EXISTS, FULL JOINs and parameterized procedures now answer over the wire, node-verified equal to the engine. Slice 14 adopted EXECUTE PROCEDURE at both wire sites with first-suspended-row semantics, and RUNTIME errors SURFACE (div-zero/overflow ship the engine's vectors). Slice 16 added blr_recurse - the fixpoint (anchor seeds, each wave bound at the recursion's own context feeds the stream-less recursive branch, breadth-first, the engine's 1024 depth cap as an ERROR), so recursive procedures serve BLR-first over the wire one slice after the compiler learned to emit them; the tower's framing law: TWO rses deep, the inner one closing with its OWN end (unlike union and window). Slice 15 added GEN_ID/NEXT VALUE FOR through an IN-MEMORY overlay with the persistence boundary drawn explicitly — the Request carries uses_generators, the wire path excludes such bodies (its persisting interpreter serves them), and the gate pairs each side with a sequence RESTART (qa/serve-real-exeproc.sh 10 + qa/exe-run-blr.sh 111 checks) |
| optimizer | query-optimizer-and-execution.md | `fire-crab-opt` | **first slice converted — ACCESS-PATH SELECTION against ORACLE NUMBER FIVE: the engine's own PLAN**. `SET PLANONLY ON` makes the engine PREPARE a statement and print the plan it chose — which index fetches the rows, whether an ORDER BY rides an index's order or needs a sort — without executing anything; `fcopt plan <db> "<sql>"` must print the SAME LINE. Converted rules, all probe-found: NATURAL when nothing is index-matchable (no WHERE, an unindexed column, `<>`, IS NOT NULL, a leading-wildcard LIKE, or an OR with ANY unmatchable branch — one bad disjunct spoils the whole clause); INDEX (a, b, ...) for matchable conjuncts or a fully-matchable OR, listed in INDEX-ID order (`=`, ranges, BETWEEN, IS NULL, STARTING WITH, prefix LIKE); ORDER \<idx\> — navigation instead of a sort — only when the ORDER BY column has an index whose DIRECTION MATCHES (the engine took the descending twin for ORDER BY ... DESC and fell to a sort when none existed) and any predicate is matched by that same index; SORT (...) wrapping otherwise. The select list provably does not influence the plan (COUNT(*) and a column list plan identically). Joins, unions, subqueries, multi-table FROMs, multi-column ORDER and compound-index matching refuse by name; cost/selectivity arithmetic is NOT converted — where the engine's choice depends on statistics rather than structure, the slice refuses rather than guesses. Slice 2 added TWO-STREAM JOINS: the inner stream must reach its rows through the join key's index, so the optimizer SWAPS the SQL order when only the first stream's key is indexed; when NEITHER is indexed the engine HASHES (FB5+) — except under an OUTER join, whose preserved side must drive and which keeps a natural nested loop; TYPE FAMILIES gate index use (a VARCHAR = INTEGER join hashes despite an indexed side — and mixing catalog RDB\$FIELD_TYPE codes with ODS descriptor dtypes, two different numberings, was the bug it caught); the driving stream keeps its own access (WHERE index, navigable ORDER, else NATURAL), and a conjunct with BOTH sides qualified is the join key rather than a filter. Slice 3 added COMPOUND-INDEX matching by the LEADING-SEGMENT rule (an (X, Y) index serves a predicate on X but not on Y alone) — the same prefix rule governing navigation, so a multi-column ORDER BY rides an index exactly when it is a PREFIX with matching directions — and CHAINS of three-plus streams (each stream after the first indexed on its own ON clause's key); Slice 4 CONVERTED the engine's EQUIVALENCE-CLASS reasoning: every equi-join predicate feeds a class, a stream is reachable when it has an index on a column of a class it shares with an already-placed stream, and the engine tries drivers in SQL order keeping the rest in SQL order — which is why an unindexable link ends up DRIVING (no other arrangement completes). The ORDER BY transfers through a class as well (ORDER BY B.UID navigates A's index on A.ID). One planner now serves all inner joins — the two-stream swap was the same rule at n = 2 — leaving the HASH fallback and the un-reorderable OUTER join as the only special cases. Slice 5 MEASURED the cost boundary instead of assuming it: slices 1-4 verified their join rules on EMPTY tables, but a POPULATED database plans differently (3000 rows against 5 drives the SMALLER stream regardless of SQL order; 3000 against 50 or more HASHES even with both sides indexed) — so a join whose streams hold ANY rows is now REFUSED with its cardinality named, while single-table paths stay unconditional (verified structural at 3000 rows). The gate gained a populated-database phase whose refusals RECORD the engine's own plan beside them. Slice 6 converted the ENGINE'S OWN CARDINALITY ESTIMATE (DPM_cardinality, dpm.epp:262 - data-page count, first-page record sampling, the exact-count-on-one-page rule, and the pages × usable-space / recordSize estimate; it reproduces the engine's imprecision faithfully, 500 rows estimating as 628) and the join decision's cardinality BANDS, mapped by probing a 6×6 size grid: a tiny driver keeps SQL order (the engine is pessimistic about relations that look empty at prepare time - InnerJoin.cpp:217), a tiny inner side makes it SWAP, both-large HASHES, and hash plans list the LARGER stream first because it PROBES while the smaller is hashed. The band between refuses, naming both cardinalities - that is where hashCost ≤ loopCost turns on Retrieval.cpp's index costs. Slice 7 CLOSED the cost model after one decisive experiment: running SET STATISTICS on every index and re-probing changed the WHOLE grid, revealing that slice 6's bands were never the law but the formulas' behaviour on STALE statistics. Converted: index selectivity from RDB\$INDICES.RDB\$STATISTICS, the indexed-retrieval cost (DEFAULT_INDEX_COST 3 + selectivity × cardinality for the scan, the same again for the records - Retrieval.cpp:1147/:384), the nested-loop cost per outer row (InnerJoin.cpp:192), the hash cost (unfiltered inner scan + hashing at MEMCOPY+HASHING + per-outer-row probe and copies, InnerJoin.cpp:229), the cheapest-of-three choice, and avoidHashJoin (a side that looks empty or single-rowed at prepare time is never hashed). With FRESH statistics the model is EXACT on ALL 36 grid cells; a zero selectivity on a populated index is refused with the remedy named (SET STATISTICS makes it plannable), and the stale grid gives 4 exact, 32 refused, ZERO wrong. **SLICE 8 converts OUTER JOINS**: an inner chain FLATTENS into one JOIN list, but an outer join makes a plan NODE and the chain NESTS - `JOIN (JOIN (A NATURAL, B INDEX), C INDEX)` - because the preserved side and its optional side are one result later joins see as a unit; an INNER join at the head is still free to swap before the outer join wraps it. Two silent wrong answers fell out of the probes: RIGHT JOIN is LEFT with the sides EXCHANGED (the preserved side drives, so `A RIGHT JOIN B` plans B-driving), and FULL JOIN is BOTH directions at once, printed `JOIN (JOIN(A,B), JOIN(B,A))` - a plan showing one half answers a different question. And a two-stream join whose BOTH keys are indexed is now REFUSED rather than answered by SQL order: probed, the engine drives the SAME stream in both SQL orders, deciding by cost and by `StreamInfo::cheaperThan` (independence, previousExpectedStreams, baseCost - Optimizer.h:895), which this crate has not converted, so the old answer was right only by accident. RIGHT/FULL inside a chain refuse. **SLICE 9 converts the RETRIEVAL-COST law behind join orders**: Retrieval.cpp:371 prices a UNIQUE equality lookup at a FIXED `DEFAULT_INDEX_COST + 1` = 4, explicitly "independent from a possibly outdated statistics", while a non-unique one costs `3 + cardinality * selectivity` - so on an unanalysed database the UNIQUE lookup is the DEARER one and the engine drives the stream whose inner lookup is non-unique (probed identical in both SQL orders), and a chain drives from its FAR end because two non-unique lookups beat two unique ones. IndexInfo gained RDB$UNIQUE_FLAG; the arrangement search now places streams by REACHABILITY rather than index order (which had been discarding the far-end arrangement before it could be costed); and slice 8's both-keys-indexed refusal is lifted. A FIFTH gate database with ASYMMETRIC index kinds - a primary key against a plain index - is what exposed all of it (qa/opt-plans.sh, 87 checks across five databases) |

## Phase 5 — the outside faces

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| wire protocol `src/remote/` + SRP `src/auth/SecureRemotePassword/` | firebird-wire-protocol.md, security-architecture.md | `fire-crab-wire` | **fire-crab logs in** — XDR framing, op_connect negotiation, SRP-256 authentication (from-scratch SHA-1/SHA-256/bignum-modpow, proof M pinned to the reference), Arc4 wire encryption, op_attach/op_detach, all over real TCP. Differentials: negotiated version matches the reference clients (qa/diff-wire.sh); the engine records the attachment as Srp256/Arc4 and rejects wrong credentials with isc_login (qa/diff-login.sh). **The statement pipeline works**: op_transaction, op_allocate_statement, op_prepare_statement, op_execute, op_fetch, op_free_statement, op_commit, over the encrypted channel; a single-BIGINT query round-trips and its decoded value matches isql (qa/diff-query.sh). General multi-column, multi-row SELECTs (integer + text) now match isql row-for-row (qa/diff-wire-select.sh). **Framing correction:** this is a wire *client* of the C++ engine - it validates the wire codec (XDR, SRP-256, message/BLR formats) against the real server, and is the groundwork every op the server side needs. firebird-qa drives a *server*, so the suite becomes applicable only once the SERVER half is converted (accept, server-side SRP, op dispatch into the engine). That is the honest next milestone; the client proves the protocol understanding it will be built on. **DML with RETURNING** is served: INSERT/UPDATE/DELETE ... RETURNING <columns>, planned WITHOUT the clause (so every default, NOT NULL, CHECK, FK and index path is the unadorned statement's) and typed `isc_info_sql_stmt_select` because Firebird 5 makes it a CURSOR - an UPDATE touching three rows returns three, and the type is a DISPATCH: node-firebird fetches all rows for a select and one for an exec_procedure. An INSERT returns the stored row (defaults filled in), an UPDATE the row AFTER the update, a DELETE the row as it WAS. `NEW.`/`OLD.` qualifiers are REFUSED because the engine refuses them (they are PSQL trigger contexts, not DSQL) - fire-crab accepted them at first and so returned rows for a rejected statement AND wrote a row the engine never wrote; the gate caught it only because it compares the TABLE afterwards, not just the rows. An expression in the list refuses at PREPARE so the write never happens (qa/serve-real-returning.sh, 14 checks, twin servers on identical databases) plus **where NULL sits**: `ORDER BY ... NULLS FIRST|LAST` (the default is not a position but a RANK - NULLs are LOW, so first ascending and last descending - while an explicit clause states a position that does NOT flip with the direction, making the four combinations four orders), and `IS [NOT] DISTINCT FROM`, desugared at parse time like BETWEEN and IN. `A IS DISTINCT FROM 5` is `A IS NULL OR A <> 5` and NOT `NOT (A = 5)` - under three-valued logic the negation drops every NULL row while the engine returns it, and the gate keeps all three predicates side by side to show it plus **ORDER BY <EXPRESSION>**: arithmetic, function calls and CASE keys, computed once per row in a decorate-sort pass so an expression that raises fails the FETCH rather than silently reordering; the item parser now strips the trailing [ASC|DESC] and [NULLS FIRST|LAST] clauses BEFORE deciding what the head is, because matching on token counts cannot distinguish `AMT + 1` from `ID NULLS LAST` (qa/serve-real-nulls.sh, 25 checks). **SUBQUERIES IN A DML WHERE**: `UPDATE`/`DELETE ... WHERE col IN (SELECT ...)`, `NOT IN`, and a scalar subquery on the right of a comparison. The predicate was never missing - the SELECT path had answered these shapes for slices - what the DML planners lacked was the LIFTING pass that evaluates each subquery once and folds its answer back into the token stream before the predicate parser sees it; adding that one pass to both planners brought every form in at once (qa/serve-real-dmlsubq.sh, 17 checks, mostly TABLE comparisons: a DML returns no rows, so a wrong filter deletes the wrong rows with an identical reply). **An AGGREGATE inside a scalar subquery**: `WHERE AMT > (SELECT AVG(AMT) FROM T)`, in a SELECT and in a DML alike - and with it MIN/MAX over TEXT, COUNT(DISTINCT col), AVG(<expression>) and a NUMERIC source's SCALE, because the subquery stopped folding through the prepare-time integer helper and now builds a one-item, no-key GROUP: a scalar aggregate IS one group of one item, so it is the same code `SELECT AVG(NUM) FROM T` already ran. The fold itself had been dropping approximate values silently (a DOUBLE answered None to the exact decomposition and contributed nothing, so AVG over a full column folded to NULL); it now promotes to an approximate fold the way the engine's descriptor arithmetic does, and refuses a DOUBLE source in the subquery exactly as the top-level planner refuses it (qa/serve-real-subqagg.sh, 30 checks, every fixture row placed so the engine's average and a plausible wrong one select DIFFERENT rows - a subquery's value is a number nobody ever sees) |
| services (`src/jrd/svc.cpp`) | services-api.md | `fire-crab-svc` | **first slice converted — the buffer grammars and the info responses, with the ENGINE'S OWN `fbsvcmgr` in the loop in BOTH directions**. The Services API is a second protocol inside the first: attach to `service_mgr` instead of a database and every request and answer is a byte buffer of tagged items — no statements, no BLR, no rows — which is why gbak, gfix, gstat, nbackup and every driver's service class are all this one interface. Converted: the FOUR grammars the same-looking bytes follow (`ClumpletReader`'s kinds — attach SPB tagged with 1-byte lengths, send items with 2-byte lengths and bare control tags, receive items bare, start SPB a per-action state machine), the TWO answer shapes chosen per item (`INF_put_item` strings `[tag][u16 len][bytes]` vs `ADD_SPB_NUMERIC` bare 4-byte numbers — nothing in the bytes says which, so `item_is_numeric` carries that knowledge), the `isc_info_svc_svr_db_info` CLUSTER (bare opening tag, two numerics, dbnames, `isc_info_flag_end`), and the TRUNCATION rule, whose comparison is `>=` so an n-byte answer needs n+5 bytes of buffer and the last byte is never used (probed live: the engine truncates its 35-byte banner at 39 and fits at 40 — the first implementation used `>` and fit an answer the engine would have cut). Differentials: `fcsvc` and `fbsvcmgr` decode the live server's answers identically item for item; `fbsvcmgr` — the engine's own C++ tool — reads fire-crab's service manager including the cluster, with nothing of ours in the reading path; the truncation boundary is measured on the engine and required of fire-crab offline AND over the wire; the grammar pins are re-captured from `fbsvcmgr`'s real buffers on every run. Refusals are converted too: an unimplemented info item draws `isc_wish_list` (335544378) from BOTH servers (svc.cpp's `query2` default arm raises, it does not skip), and a service ACTION is now refused instead of acknowledged — the old server answered `op_service_start` with a clean response, i.e. "your backup is done", for a backup it never ran (qa/svc-info.sh, 19 checks). Frontier: the actions themselves (gbak/gfix/gstat behind an SPB) and the `isc_info_svc_line`/`to_eof` output stream. **SLICE 2 closed both of those for one action**: `isc_action_svc_db_stats` with `isc_spb_sts_hdr_pages` is served by reading the file and streaming gstat's own report text - the conversion of `PPG_print_header` - so the ENGINE'S OWN `gstat`, pointed at fire-crab, prints statistics identical to what it prints reading the file itself. The report needed header fields fire-crab had never decoded (the implementation triple through `DbImplementation`'s NAME TABLES, the creation date, the shadow count, and the VARIABLE HEADER CLUMPLETS at offset 148 where the sweep interval lives) and three text laws that are easy to get backwards (`Flags` is the PAGE's flags, not `hdr_flags`; the `Attributes` LABEL prints unconditionally while its value and newline do not, leaving a dangling line for a database with no attributes; and `dialect 1` means the header carries no dialect information). The OUTPUT STREAM law was read off the live wire before being written: `isc_info_svc_line` returns a line with its newline REPLACED BY A SPACE, so a blank line is a single space (`3e 01 00 20 01`) and a ZERO-length item means end-of-stream, while `to_eof` returns raw bytes with newlines intact. The engine's own option validation is converted with its own facility-coded number - `-h` combined with another analysis is gstat message 38 = gds 336920614 from both servers - and the analyses fire-crab cannot compute are refused with isc_wish_list while the real server performs them, so the refusal is provably ours (qa/svc-stats.sh, 16 checks) |
| events (`src/jrd/event.cpp`) | firebird-events.md | `fire-crab-evt` | **first slice converted — the counter-and-interest model with its transaction coupling, differentially tested against the LIVE engine through the paper's own event client**. An event is not a message but a COUNTER (`evnt_count`, event.h:105), and a client's interest carries the count it has already seen (`rint_count`); every observable law follows: delivery is COMMIT-TIME (a post inside a transaction changes nothing visible), ROLLBACK swallows posts (they were never counted), several posts of one name COALESCE into ONE delivery carrying the new counter (a client learns "it happened, and the counter is now N" — never three messages), a fired interest comes DOWN until the client re-queues, and a fresh interest below the current count fires AT ONCE, which is how a subscriber learns its baseline. The gate is a SEMANTIC differential: the paper's node client (samples/nodejs/events.js) prints those facts against a real server over a real auxiliary connection, `fcevt replay` prints them from the converted table, and the two must agree — including the counter DELTA, the invariant that survives a database's history. The shared-memory arena (self-relative queues, process/session blocks, the watcher thread) and the wire delivery path stay unconverted as transport (qa/evt-semantics.sh, 4 checks) |
| security (`src/auth/`) | security-architecture.md | `fire-crab-auth` | **first slice converted — SRP-6a in BOTH plugin variants, with the ENGINE'S OWN STORED VERIFIERS as the oracle**. The crate is the extraction the engine's own architecture implies: `src/remote/` only CARRIES auth messages, a plugin decides, so the SRP computation (and the from-scratch SHA-1/SHA-256/bignum/Arc4 under it) left `fire-crab-wire` for its own crate and now serves four roles — client (fire-crab logs in to the engine), server (`fcwire serve` accepts real drivers), USER MANAGER (the verifier `CREATE USER` stores, new here), and oracle (`fcauth`). Four deviations from RFC 2945/5054 are converted and pinned: u = SHA1(A|B) over MINIMAL bytes; n1 = H(N)^H(g) computed as a modPow (the comment says xor, the code raises to a power); K = SHA1(S) even in Srp256, so Arc4 is keyed by 20 bytes in both plugins and "Srp256 means SHA-256 everywhere" is a login that can never succeed; and every number travels as `BigInteger::getText` — minimal uppercase hex, no leading zero — which for the salt means ONE USER IN SIXTEEN has a 63-character salt. The gate hunts that user on purpose (ALTER USER until the engine hands out a leading-zero salt, 9-11 tries in practice) and shows only the minimal form reproduces the stored verifier while the padded and raw forms do not. Reading `plg$srp.plg$srp` needs no SQL and cannot use it — `databases.conf` ships `security.db` with RemoteAccess = false and a local attach collides with the running server — so `fcauth stored` reads the security database with fire-crab's OWN ODS decoder, as bytes (OCTETS columns). Live handshakes against the real engine gate all four verdicts: accepted for Srp256, isc_login (335544472) for a wrong password and for an unknown user, isc_login_error (335545106) when only `Srp` is offered to a default `AuthServer = Srp256` server — recorded as the engine's answer rather than papered over. Cross-checked digit for digit against node-firebird's independent SRP for both plugins, which is how the getText wire form was caught (qa/auth-srp.sh, 13 checks). Not converted: Legacy_Auth (DES over 8 characters), ChaCha wire crypt, identity MAPPING |

## Reference material per row

For every subsystem above, the paper repo also carries **verified hands-on
samples in five languages** (C++ OO-API, fb-cpp, node-firebird, rsfbclient,
fbintf) whose outputs are known-good expected values for differential tests —
e.g. the blr samples' byte dumps, the transactions samples' conflict error
chains, and the on-disk samples' header/census values used by this repo's QA
today.
