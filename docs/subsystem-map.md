# Subsystem map: C++ engine → paper document → Rust conversion

The conversion's chart. Each row links the C++ source directory, the
companion-paper document that explains it (the recommended reading *before*
the source), the planned crate, and the status. Order within phases is the
planned conversion order; the criterion is always "what can be
differential-tested next with the least new machinery" (see
[methodology.md](methodology.md)).

Paper document links are relative to
[conceptual-architecture-for-firebird-paper](https://github.com/mariuz/conceptual-architecture-for-firebird-paper).

## Phase 1 — storage, bottom-up (in progress)

| C++ source | Paper document | Crate / module | Status |
|---|---|---|---|
| `src/jrd/ods.h` (pag, header_page, tx_inv_page) | on-disk-structure.md | `fire-crab-ods` {pages, header, tip} | **done** — differential vs gstat, 123 dbs |
| `src/jrd/sqz.cpp` (record RLE) | on-disk-structure.md § records | `fire-crab-ods::sqz` | **done** — round-trip incl. FB4 forms |
| `src/jrd/ods.h` (page_inv_page / PIP) | on-disk-structure.md | `fire-crab-ods::pip` | **done** — bitmap + capacity formula tested |
| `src/jrd/ods.h` (pointer_page, data_page, rhd/rhde + flags) | on-disk-structure.md, transactions-and-concurrency.md | `fire-crab-ods::{pointer,data}` | **done** — record walk diffs vs live SELECT COUNT(*) (qa/diff-select.sh), 0 to 200k rows OK |
| record field decode via RDB$FORMATS | on-disk-structure.md, metadata-cache.md, catalog-bootstrap.md | `fire-crab-ods::format` | **done** — descriptors, null bitmap, dtype decode, blob-id resolution + level 0/1 segmented blob assembly, hardcoded system format for the bootstrap; full-row differential vs live SELECT (qa/diff-rows.sh) |
| `src/jrd/btr.cpp`, `btn.h` / index_root_page + btree_page + node encoding | indexing-and-full-text-search.md | `fire-crab-ods::btr` | **done** — leaf-level walk with prefix decompression; index-order differential vs live ORDER BY (qa/diff-index.sh), identical order at 200k rows through a multi-level tree |
| `src/jrd/blb.cpp` / blob_page | blob-handling.md | `fire-crab-blb` | planned |

## Phase 2 — the transaction system

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| `src/jrd/tra.cpp` TIP chain, `vio.cpp` version rules, `sqz.cpp` deltas | transactions-and-concurrency.md | `fire-crab-ods::tra` | **done** — TIP-chain state lookup, delta back-version reconstruction (Difference::apply), committed-only MVCC visibility walk; differential vs live SELECT on a file frozen mid-uncommitted-work (qa/diff-mvcc.sh) |
| `src/jrd/vio.cpp` GC/sweep (VIO_chase_record_version, cannotGC vio.cpp:1663) | garbage-collection-and-sweep.md | `fire-crab-ods::gc` | **done** — classifies collectable versions (expunge path + back-chain path) against the oldest-snapshot threshold; prediction differential vs live `gfix -sweep` (qa/diff-sweep.sh), predicted removal == actual removal, 210 versions across both paths |
| `src/lock/lock.cpp` | lock-manager.md | `fire-crab-lck` | planned |

## Phase 3 — cache and physical I/O

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| `src/jrd/cch.cpp` (page cache, latching) | page-cache-coherency.md, careful-writes-and-crash-safety.md | `fire-crab-cch` | planned — careful-writes precedence is THE correctness gate; crash-harness differential (kill mid-write, compare recovery) |
| `src/jrd/pag.cpp`, `src/jrd/pio_unix.cpp` | on-disk-structure.md | `fire-crab-pio` | planned |

## Phase 4 — language: BLR, DSQL, the executor

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| BLR decode (`par.cpp` structure, `blp.h` + gds.cpp operand table) | blr-intermediate-language.md | `fire-crab-ods::blr` | **done** — operand-atom walker + verb table (171 verbs) converted from the engine's own printer; verb-token differential vs isql `SET BLOB ALL` (qa/diff-blr.sh), every decodable blob matches token-for-token, unknown verbs reported not guessed |
| `src/dsql/` (SQL → BLR) | grammar-and-parser.md, dsql docs | `fire-crab-dsql` | **forty-two slices converted + differential-tested against FOUR of the engine's own oracles (seven catalog stores)** — the view-shaped SELECT compiles to BYTE-IDENTICAL BLR, verified against `RDB$VIEW_BLR` (the artifact the engine's DSQL itself stores for the same statement). Covered: WHERE booleans (AND/OR/NOT with the engine's De-Morgan folding, comparisons, IS NULL, BETWEEN, LIKE), VALUE EXPRESSIONS (add/subtract/multiply/divide/negate/concatenate — a sign before a numeric literal folds into it), IN lists (blr_in_list + u16 count; NOT IN keeps blr_not), MULTI-STREAM RSEs (comma-FROM and INNER JOIN ... ON as a nested blr_join), aliases (blr_relation2, the alias UPPERCASED IN DOUBLE QUOTES), qualified fields by context (bare fields in multi-stream refuse — catalog-free), OUTER JOINS (blr_join_type 1/2/3 after the streams; absent for INNER), JOIN CHAINS (left-nested: each join node holds the previous as its first stream slot), INT64 literals (blr_int64), and the first BUILT-IN FUNCTIONS: UPPER/LOWER, CHAR_LENGTH/OCTET_LENGTH (blr_strlen + length-type byte), SUBSTRING (0-based start compiled as an UNFOLDED subtract(from,1)), TRIM (where + spec bytes) — an unknown name before `(` refuses, never a field — and CAST (blr_cast + probed dsc layouts: NUMERIC(p≤4) short but DECIMAL(p≤9) always long) with the CONDITIONALS: searched CASE/IIF = ONE blr_cast over a blr_value_if chain typed by the probed unification law (max int-digits + min scale, dtype that fits — long⁰∪long⁻¹ widens to int64), simple CASE = blr_decode, COALESCE = blr_coalesce (both wrapper-free), NULLIF = cast(value_if(a=b, NULL, a)) typed from its branches only; field branches under a cast wrapper refuse — and the SUBQUERY PREDICATES: EXISTS = blr_any over one rse (subquery WHERE = its boolean, select list traceless), SINGULAR = blr_unique, IN (SELECT)/ANY/SOME/ALL = blr_ansi_any/_all with a double-nested rse (the outer rse's single stream IS the subquery's rse; the comparison is the outer rse's boolean); negation flips the quantifier and inverts the comparison (NOT IN = ansi_all+neq); subquery streams join the context numbering but stay invisible to outer bare names — plus DISTINCT (blr_project: the one place the select list leaves a trace), scalar subselects (blr_via(blr_singular(rse), value, null)), derived tables (an rse in the stream slot, alias text `"X" "PUBLIC"."T"`, ONE shared context), and UNION [ALL] (blr_union stream with per-branch rse + blr_map; distinct form adds blr_project over blr_fid) (qa/dsql-view-blr.sh, 127 checks incl. refusals). Slice 7 opened oracle number two — `RDB$PROCEDURE_BLR`: `compile_procedure` emits a whole `FOR SELECT ... DO SUSPEND` body byte-identically (message with dsc+null-flag pairs and the EOF short, declares with NULL-inits, stall, labels, blr_for whose stream is CONTEXT 0 — procedures number from 0, views from 1 — ORDER BY as blr_sort asc/desc after the boolean, twin sends via blr_parameter2), unlocking what views cannot hold — and AGGREGATES + GROUP BY: blr_aggregate as a STREAM with its own context over the source rse (WHERE stays the source's boolean), blr_group_by in clause order vs blr_map in select-list order (probed to differ), agg verbs 0x53–0x57/0x5D, HAVING as the outer rse's boolean over blr_fid slots (equal aggregates REUSE their map slot, fresh ones append), ORDER BY sorting fids — and INPUT PARAMETERS: message 0 (dsc + null-flag pairs, no EOF slot), the loop under blr_receive 0, `:name` compiling to blr_parameter2(0, 2i, 2i+1) straight as a value anywhere in the FOR select — plus the SINGULAR `SELECT INTO` (blr_for over blr_singular, no label 1, SUSPEND a sibling send), FIRST/SKIP (rse sub-clauses between streams and boolean), and the DISTINCT aggregate verbs (COUNT/SUM/AVG dedicated; MIN/MAX fold to plain) (qa/dsql-proc-blr.sh, 48 checks incl. refusals). Slice 11 opened oracle number three — `RDB$TRIGGER_BLR`: compile_trigger emits whole trigger bodies byte-identically (the leanest wrapper: begin, label 0, DOUBLE begin, statements, three ends; OLD=ctx 0, NEW=ctx 1 as pseudo-streams; assignments, IF with a bare blr_end for a missing ELSE, nested blocks as double begins; the header leaves no BLR trace) — and THE DML VERBS: INSERT = blr_store(relation, assignments, column list required), DELETE = blr_for over a marks(1,4)-stamped rse + blr_erase, UPDATE = the same loop + blr_modify(org, new) with the new-record context allocated BEFORE the rse stream's; SET targets write new, sources and WHERE read org — and PSQL CONTROL: DECLARE [VARIABLE] with initialisers (triggers group declares-then-inits; procedures interleave — both probed), local-variable reads/assignments (blr_variable), WHILE = label N + blr_loop + if(cond, body, blr_leave N) with encounter-ordered labels, INSERTING/UPDATING/DELETING = eql(blr_internal_info(6), 1/2/3) composing under NOT/AND/OR (qa/dsql-trig-blr.sh, 34 checks incl. refusals). Slice 14 UNIFIED the surfaces: procedure bodies run the same statement machine as triggers — outputs ARE variables (locals continue the numbering), SUSPEND is a statement, RETURNS is optional (one-slot EOF message), bare names resolve vars-then-inputs outside stream scopes but stay columns inside them, and bodies mix FOR SELECT / SELECT INTO / DML / IF / WHILE / SUSPEND freely. Slice 15 added the CALL surface: EXECUTE PROCEDURE = blr_exec_proc (counted name, u16 in-count+values, u16 out-count+variables), EXCEPTION = blr_abort+2+name, EXIT = blr_leave 0 (the wrapper's label); (FOR) SELECT works in trigger bodies (stream after OLD/NEW), and the aggregate-context law generalised to stream+1 at any context (qa/dsql-proc-blr.sh 67 + qa/dsql-trig-blr.sh 38 checks incl. refusals). Slice 16 opened oracle number four — column BLR: DEFAULT clauses (RDB$DEFAULT_VALUE) and COMPUTED BY expressions (RDB$COMPUTED_BLR), both `blr_version5 <value> blr_eoc` — defaults as narrow as the engine's grammar (literals/NULL/CURRENT_DATE-TIME-TIMESTAMP), computed columns taking the whole expression surface at an anonymous context 0. Slice 17 added domain defaults (RDB$FIELDS), expression indexes (RDB$INDICES.RDB$EXPRESSION_BLR — the computed-column frame again) and CHECK constraints (the engine's own system triggers: blr_if over the NEGATED condition, blr_abort gds_code 'check_constraint', fields at ctx 1) — and closed a latent gap the CHECK battery exposed: non-integer IN-list items are CAST to the left side's catalog type, so they refuse now. Slice 18 added the seventh store — domain validation (RDB$VALIDATION_BLR: the RAW un-negated boolean with VALUE = blr_fid(0,0), a different shape from table CHECKs) — plus GEN_ID (blr_gen_id: name + increment) vs NEXT VALUE FOR (blr_gen_id2: name alone — two verbs for one concept, chosen by syntax) and POST_EVENT (blr_post + value). Slice 19 added error handling: WHEN-carrying blocks = blr_block (guarded begin, blr_error_handler + code — ANY/EXCEPTION-name/GDSCODE-name — handler statement, end) while plain blocks stay double-begins in both body kinds; ROW_COUNT = internal_info(5). Slice 20 converted UPDATE OR INSERT (a begin: marks-stamped modify-loop with blr_equiv on the MATCHING column, then if(row_count=0, store); contexts store/modify-new/rse-org in that order; MATCHING required and single-column), added CURRENT_CONNECTION/CURRENT_TRANSACTION (internal_info 1/2), and settled multiple handlers (sequential sections) and handler-body blocks (blr_block nested again) — flips ten and eleven. Slice 21 added autonomous transactions (blr_auto_trans), multi-column MATCHING (equivs left-nested under blr_and), WHEN SQLCODE (code 1 + i16 — flip twelve), and CURSORS (blr_dcl_cursor with the cursor name riding the relation2 alias, blr_derived_expr outputs, blr_cursor_stmt open/close/fetch) — plus a pinned ordering law: variable inits DEFER past following cursor declarations. Slice 22 COMPLETED the cursor surface: WHEN SQLSTATE (code 8 + counted string — flip thirteen), handlers carrying their OWN handlers (the nested block keeps its error-handler section — flip fourteen), cursor table ALIASES (the relation2 alias string becomes `"CX" "A"` — cursor name + table alias — displacing the schema-qualified table) with qualified columns resolving through the one stream, AGGREGATE cursors (blr_aggregate nested at ctx+1, claiming a SECOND context slot; outputs and fetch-sources are BARE blr_fid slots — no derived_expr wrapper; the engine demands AS names), and POSITIONED DML — DELETE ... WHERE CURRENT OF = blr_erase at the CURSOR's own context, UPDATE = blr_modify from it to ONE fresh slot, both stamped blr_marks(1, 1) TRAILING the verb where a DML loop's marks(1, 4) LEAD its rse; the INTO-less FETCH that positions them carries an empty begin/end. Slice 23 added MERGE — one probed sentence: `for(marks(1, 6), rse(join2(source@0, target@1, [LEFT], ON), [branch-union boolean]), if(missing(dbkey(target)), store, modify/erase + marks(1, 2)))` — the join LEFT only when a NOT MATCHED branch needs unmatched rows (matched-only merges are INNER with NO rse boolean), the boolean ORing branch conditions in canonical order (SQL branch order leaves no trace), branch contexts allocating UPDATE's new record then INSERT's store, the store re-emitting the aliased target — plus FOR SELECT ... AS CURSOR (the labeled FOR loop whose relation2 alias carries the cursor name exactly like a DECLAREd cursor's; INTO optional, its sources derived_expr-wrapped; WHERE CURRENT OF at the FOR's own context, body-scoped) and cursors in TRIGGER bodies (declarations keep their source slot among the grouped declares — the trigger flavor of the deferral law — numbering past OLD/NEW). Slice 24 added CONDITIONAL MERGE branches (WHEN [NOT] MATCHED AND <cond> joins the rse boolean's branch term — and(not(missing), cond) / and(missing, cond), the bare cond for matched-only — and nests if(cond, action, bare end) in the branch's slot), SCROLL cursors with the FETCH directions (blr_scrollable before the dcl_cursor rse; FETCH <dir> FROM = cursor_stmt sub-verb 3 + direction byte 0–5 + an offset value, blr_null unless ABSOLUTE/RELATIVE; only NEXT is legal unscrolled), DML RETURNING ... INTO — THREE shapes for one clause: INSERT = blr_store2 with a second returning begin, UPDATE = blr_modify2 under a SINGULAR rse with returning reading the NEW record, DELETE = no erase2 at all but a begin(returning-assigns, plain erase) under a singular rse — and EXECUTE STATEMENT (plain = blr_exec_sql + the literal; [FOR] ... INTO = blr_exec_into, u16 out-count, the sql, flag 1 singleton / flag 0 + the labeled DO statement, the variables LAST). Slice 25 added MULTI-BRANCH MERGE — branches of one kind form an if-else chain in SQL order, each conditional branch if(cond, action, <next>) with the next if filling the else slot BY POSITION, the last conditional getting a bare end, an unconditional LAST branch filling the else directly; the rse boolean ORs kind-terms built from left-nested or-chains (and(not(missing), or(c1, c2, ...)) / and(missing, or-chain)), each simplified to its bare missing-test when the kind has an unconditional branch; contexts allocate BY KIND in branch order (every matched UPDATE claims a new-record slot, then every INSERT its store) — PARAMETERIZED EXECUTE STATEMENT (('sql') (vals) = the FULL blr_exec_stmt with tag-prefixed clauses in fixed order: 1 in-count, 2 out-count, 3 sql, 4 the FOR form's DO statement, 11 input values, 13 output variables, blr_end; the literal no-param forms keep their compact verbs) and WITH LOCK (blr_writelock between the stream and the boolean — in FOR SELECT, DECLAREd cursors and AS CURSOR loops alike). Slice 26 added SUBROUTINES: DECLARE FUNCTION/PROCEDURE = blr_subfunc_decl/blr_subproc_decl — counted name, type 0, a flag byte (deterministic for functions, SELECTABLE for procedures), u16-counted parameter-name lists (the function's return slot an UNNAMED output), then the WHOLE inner body's BLR as a u32-counted blob, compiled by the same body machinery on a fresh parser; RETURN <expr> = begin(assign the unnamed slot 0, the no-EOF send, blr_leave 0); calls are blr_invoke_function value expressions / blr_invoke_procedure statements with the sub id clause (4 sub, 3 counted name) and u16-counted args; a subroutine's INPUTS reserve variable slots (locals number past them — top-level bodies don't) and a void subroutine drops blr_stall — plus NAMED EXECUTE STATEMENT parameters (tag 12: counted name + value each) and the ON EXTERNAL / AS USER / PASSWORD / ROLE modifiers (tags 5/6/7/14; any modifier forces the full blr_exec_stmt form) — AND A LATENT FIX: procedure LOCALS group (declare, declare, init, init) where outputs interleave; sixteen slices of single-local bodies read as interleaving until a two-local probe settled it. Slice 27 opened STREAMS INSIDE SUBROUTINES: blr_relation3 — counted schema PUBLIC, counted EMPTY package (the mystery byte, read from RelationSourceNode::genBlr), the name, then the alias slot ALWAYS present (relation2's alias string — quoted alias or cursor-name string — or a counted empty), the context; one emission switch turns the whole converted surface loose in subroutine bodies (INSERT, DML loops, singular SELECT INTO, FOR SELECT, DECLAREd cursors, AS CURSOR positioned DML, UPDATE OR INSERT, MERGE — battery-verified, most never probed in subroutine clothing), and zero-arg sub-function calls compile with the argument tag at count 0. Slice 28 flipped one of the oldest refusals — ALIASED STREAMS in body statements (FOR SELECT / DELETE / UPDATE with table aliases: relation2 + quoted alias at top level, relation3's always-present alias slot inside subroutines, qualified references resolving through the unchanged field machinery, aliased aggregate sources with qualified group keys included) — and let SUBROUTINES into TRIGGER bodies: DECLARE PROCEDURE/FUNCTION takes the same grouped-declare slot cursors do, calls working from trigger statements with outer-scope arguments like NEW.ID. Slice 29 dropped the second slice-7 refusal in two slices — SUBQUERIES IN BODY STATEMENTS: EXISTS/SINGULAR, IN (SELECT)/ANY/ALL and scalar subselects in body WHEREs and assignments, the whole view-compiler machinery carried over on TWO changes (the subquery stream takes the NEXT context id at the statement's numbering — the view formula was this law at base 1 — and a `host` slot keeps the enclosing statement's stream visible to qualified names, probed on an EXISTS correlated by table name); a scalar subselect in an assignment claims ctx 0 — plus ALIASED AS CURSOR (the cursor name pairs with the table alias, `"CU" "E"`, riding relation3's alias slot inside subroutines). Slice 30 added AGGREGATE SCALAR SUBSELECTS — via(singular, rse1(aggregate at the NEXT slot over the inner rse, its WHERE INSIDE, zero group keys, a one-slot map), fid(agg, 0), null), the aggregate claiming the slot after its stream as everywhere — in assignments, WHERE comparisons, SET values and subroutine bodies; subqueries inside a CURSOR's rse stay guarded with a peculiar probed law in hand (their streams INHERIT the cursor alias string — an EXISTS stream carried `"CX" "PUBLIC"."T"`). Slice 31 CONVERTED that infection: a post-parse STAMP walks the cursor's boolean tree pairing the cursor name with each subquery stream's own alias (`"CX" "X"`) or schema-qualified name — DECLARE CURSOR, AS CURSOR and cursor-in-subroutine (relation3's alias slot) all pinned — plus DISTINCT aggregate scalars (the dedicated COUNT/SUM/AVG verbs, MIN/MAX folding DISTINCT away — the slice-10 law reaching the scalar surface). Slice 32 added JOINS IN BODY FOR SELECTs — INNER/LEFT/RIGHT/FULL chains in FOR SELECT and the singular SELECT INTO, the view's left-nested chain at body numbering (ON at the join level, WHERE at the rse level, join_type absent for INNER), the MERGE scope generalized to a stream-index RANGE with qualified-only resolution (the gate itself caught a bare column binding to the FIRST stream where the engine resolves through the catalog — a wrong-bytes hazard closed before shipping) — and PACKAGED ROUTINE CALLS: EXECUTE PROCEDURE PKG.P = blr_exec_proc2 (counted package + name, u16 counts), PKG.F(x) = blr_function2 (a count BYTE) — compact verbs, not the invoke forms subroutines take. Slice 33 completed the join story: AGGREGATES OVER JOINS (the chain inside the aggregate's inner rse with the WHERE after it, the aggregate claiming the slot after ALL join streams — the slice-8 next-slot law counting past a chain; GROUP BY/HAVING/ORDER BY on the existing map machinery) and JOINED CURSORS (both streams carrying the cursor pairing — `"CX" "A"` / `"CX" "B"` — via the slice-31 stamp; each output's derived_expr wrap naming its column's OWN stream; positioned DML on joined cursors refusing — not updatable). Slice 34 added WINDOW FUNCTIONS: `blr_window` wraps the inner rse (its WHERE inside), then a window count and per window `blr_partition_by` — context, partition keys as source fields then REMAPPED as fids into the window's own map, a sort clause, the map; passthrough columns live in the DEFAULT (empty-spec) window beside empty-spec OVER () functions, each distinct (partition, order) spec gets its own window in ENCOUNTER order claiming the contexts after the stream, maps hold items in select order with partition keys appended, outputs read per-window fids; the aggregate verbs work windowed and ROW_NUMBER/RANK/DENSE_RANK ride `blr_agg_function` (counted name + an argument count); frame extents initially guarded. Slice 35 completed the window story: LAG/LEAD/FIRST_VALUE/LAST_VALUE under blr_agg_function with TRUE argument counts (LAG/LEAD canonicalize to three — value, offset filled with literal 1, default filled with blr_null), and FRAME EXTENTS on the v4 `blr_window_win` verb — subcoded clauses (1 partition carrying the v3 layout, 2 order, 3 map, 4 extent unit RANGE 0 / ROWS 1, 5 frame bounds, 6 frame values) with its OWN blr_end; UNBOUNDED is a bound sans value, a single bound implies CURRENT ROW as frame two, and v3/v4 windows mix freely in one statement. Slice 36 added NTH_VALUE (a FROM FIRST indicator — literal 0 — appended as the third agg_function argument) and DERIVED TABLES in body FOR SELECTs (the view convention at body numbering: the inner rse in the stream slot, inner WHERE inside, outer WHERE at the rse level, ONE shared context; pass-through lists only, derived-column aliases guarded) — and PINNED derived tables in subqueries, a shape LIVE through composition since the subquery slice without a single covering test. Slice 37 added DERIVED-COLUMN ALIASES: the derived list records (outer, inner) name pairs and every outer reference — qualified or bare, in items, WHEREs, ORDER keys or correlated subqueries — TRANSLATES to the inner column at the shared context (`D.X` emits the underlying `UID`); one translation point in the field resolver, the hardcoded bare-name sites routed through it. UNION in body FOR SELECTs probed (blr_union claiming the statement's first slot, branch streams following, fid outputs) — a named refusal with its transcript; named windows guarded as an engine-side probe blocker. Slice 38 cashed the union transcript — UNION [ALL] in body FOR SELECTs: `blr_union` claims the statement's FIRST slot (a lookahead reserves it before the branch streams number), per-branch rses with their WHEREs inside and positional maps, DISTINCT appending `blr_project` over the union fids, INTO reading them — and flipped a TWENTY-SIX-slice-old refusal: INSERT..SELECT, a marks(1, 4)-stamped FOR loop over the source rse storing one row per source row, the SOURCE stream numbering first, working in procedures, subroutines and triggers. Slice 39 added WITH CTES — the engine INLINES a non-recursive cte as a derived table (the cte name becoming the relation2 alias, column aliases translating, inner WHERE inside); the parser records the body's token SPAN at WITH and expands it at the FROM reference, one cte referenced exactly once — and EXPRESSION INSERT..SELECT (source items as full value expressions via the two-phase list parse), the FIFTIETH refusal to fall; GROUP BY expressions probed and left named. Slice 40 cashed that transcript and two more flips: SELECT-ITEM EXPRESSIONS (full value surface at the stream context, bare names resolving as columns), GROUP BY EXPRESSIONS (the group list takes the raw expression, the map its BARE fields in select-item order — each item contributing its deduped fields or its aggregate verb, the slice-8 law generalized — items AND aggregate ORDER keys rebuilt over the fids; the gate itself caught ORDER BY SALARY / 1000 refusing) and MULTIPLE CTES (comma-separated WITH lists, each expanding once, pinned inside a correlated subquery). Slice 41 opened five doors: EXPRESSION SORT KEYS (raw expressions in the sort clause; bare literals refuse — the engine reads them as POSITIONS), WINDOW PASSTHROUGH EXPRESSIONS (fields into the default window's map, items rebuilt over fids), OFFSET/FETCH (the standard spelling of SKIP and FIRST, same clauses in the same order; PLAN/NATURAL/OFFSET/ROWS/ONLY joined the keyword list after PLAN was slurped as a table alias), PLAN (tbl NATURAL) (`blr_plan` + `blr_retrieve` + the stream re-emitted + `blr_sequential`, last in the rse) and PARAMETERS IN HAVING — a slice-9 refusal, thirty-two slices old, felled by the gate's own battery statement; HAVING without GROUP BY pinned as an open door. Slice 42 added DISTINCT IN BODY FOR SELECTs (blr_project over the select columns after the boolean - the view law at body numbering), PLAN (tbl INDEX (names)) (blr_indices + counted names in place of blr_sequential) and ROWS m TO n (the legacy limits desugaring to UNFOLDED arithmetic - first = add(subtract(n, m), 1), skip = subtract(m, 1)); recursive CTEs probed (blr_recurse - the recursive branch STREAM-LESS, reading the recursion context by fid) and left a named refusal with the transcript (qa/dsql-proc-blr.sh 253 + qa/dsql-field-blr.sh 39 + qa/dsql-trig-blr.sh 48 checks incl. refusals). The interpreted SQL surface inside `fire-crab-wire::server` (expressions, functions, CASE, predicates, DML, DDL, PSQL) remains its own path, documented source-by-source in [expression-surface.md](expression-surface.md); the two meet as this crate grows toward full statement compilation |
| `src/jrd/exe.cpp`, rse execution | query-optimizer-and-execution.md, request-lifecycle-code-trace.md | `fire-crab-exe` | planned |
| optimizer | query-optimizer-and-execution.md | `fire-crab-opt` | planned — differential via RDB$SQL.EXPLAIN output on identical statistics |

## Phase 5 — the outside faces

| C++ source | Paper document | Crate | Status |
|---|---|---|---|
| wire protocol `src/remote/` + SRP `src/auth/SecureRemotePassword/` | firebird-wire-protocol.md, security-architecture.md | `fire-crab-wire` | **fire-crab logs in** — XDR framing, op_connect negotiation, SRP-256 authentication (from-scratch SHA-1/SHA-256/bignum-modpow, proof M pinned to the reference), Arc4 wire encryption, op_attach/op_detach, all over real TCP. Differentials: negotiated version matches the reference clients (qa/diff-wire.sh); the engine records the attachment as Srp256/Arc4 and rejects wrong credentials with isc_login (qa/diff-login.sh). **The statement pipeline works**: op_transaction, op_allocate_statement, op_prepare_statement, op_execute, op_fetch, op_free_statement, op_commit, over the encrypted channel; a single-BIGINT query round-trips and its decoded value matches isql (qa/diff-query.sh). General multi-column, multi-row SELECTs (integer + text) now match isql row-for-row (qa/diff-wire-select.sh). **Framing correction:** this is a wire *client* of the C++ engine - it validates the wire codec (XDR, SRP-256, message/BLR formats) against the real server, and is the groundwork every op the server side needs. firebird-qa drives a *server*, so the suite becomes applicable only once the SERVER half is converted (accept, server-side SRP, op dispatch into the engine). That is the honest next milestone; the client proves the protocol understanding it will be built on. |
| services (`src/jrd/svc.cpp`) | services-api.md | `fire-crab-svc` | planned |
| events (`src/jrd/event.cpp`) | firebird-events.md | `fire-crab-evt` | planned |
| security (`src/auth/`) | security-architecture.md | `fire-crab-auth` | planned — Srp reference implementations exist in three languages in the paper's samples |

## Reference material per row

For every subsystem above, the paper repo also carries **verified hands-on
samples in five languages** (C++ OO-API, fb-cpp, node-firebird, rsfbclient,
fbintf) whose outputs are known-good expected values for differential tests —
e.g. the blr samples' byte dumps, the transactions samples' conflict error
chains, and the on-disk samples' header/census values used by this repo's QA
today.
