# fire-crab 🔥🦀

An incremental conversion of the [Firebird](https://github.com/FirebirdSQL/firebird)
database engine from C++ to Rust — started bottom-up from the storage layer,
tested differentially against the real engine from the first commit.

## What this is (and is not)

Firebird's engine is on the order of a million lines of C++ with thirty years
of accumulated correctness. **fire-crab is not a rewrite announcement** — it is
a methodical conversion experiment with three rules:

1. **Every converted piece must be verifiable against the C++ engine today** —
   not "when the project is finished". The first slice decodes real database
   files written by Firebird 6 and is diffed field-for-field against `gstat`
   on every database we can generate.
2. **The C++ source is the specification.** Every Rust struct, constant and
   algorithm carries a pointer to the file and line it was converted from, and
   the C++ `static_assert`s that pin on-disk layouts are mirrored as Rust
   tests.
3. **Documented as it happens.** The conversion steps, the decisions, and the
   dead ends live in [docs/methodology.md](docs/methodology.md); the map from
   engine subsystems to conversion status lives in
   [docs/subsystem-map.md](docs/subsystem-map.md).

The project grew out of
[a conceptual-architecture paper on Firebird](https://github.com/mariuz/conceptual-architecture-for-firebird-paper)
whose 43 subsystem documents (each with verified hands-on samples in five
languages) serve as the conversion's guidebook: each subsystem document tells
the converter what the C++ is *doing* before they read a line of it.

## Status

| Area | Crate | Status |
|---|---|---|
| ODS page structures (header, generic page, TIP) | `fire-crab-ods` | **converted + differential-tested** |
| Record RLE compression (`sqz.cpp`, incl. FB4 extended runs) | `fire-crab-ods` | **converted + round-trip-tested** |
| Page-type census / `gstat`-style tool | `fcstat` | **working** |
| PIP, pointer pages, data pages + record-version walk | `fire-crab-ods` | **converted + differential-tested vs live SELECT** |
| Record field decoding (RDB$FORMATS bootstrap, blob assembly) | `fire-crab-ods::format` | **converted + full-row differential vs live SELECT** |
| B-tree index pages + node encoding (`btn.h`) | `fire-crab-ods::btr` | **converted + index-order differential vs live ORDER BY** |
| Transaction system: TIP chain, delta versions, MVCC visibility (`tra.cpp`/`vio.cpp`) | `fire-crab-ods::tra` | **converted + committed-only-visibility differential vs live SELECT** |
| Garbage-collection / sweep analysis (`vio.cpp`) | `fire-crab-ods::gc` | **converted + prediction differential vs live `gfix -sweep`** |
| BLR intermediate language (`par.cpp`, `blp.h`) | `fire-crab-ods::blr` | **converted + verb-token differential vs the engine's own BLR printer** |
| Wire protocol - client: login + general SELECT (`src/remote/`, `src/auth/`) | `fire-crab-wire` | **fire-crab runs multi-column, multi-row SELECTs** matching isql row-for-row (integer + text). Validates the wire codec against the real C++ server |
| Wire protocol - server: accept + SRP-256 + attach + statement pipeline | `fire-crab-wire::server` | **a real third-party client (node-firebird) authenticates, arms Arc4 wire encryption, attaches and runs a query end-to-end against fire-crab**; the C++ `isql` authenticates and attaches too (see below) |
| Real query execution: `SELECT COUNT(*) FROM <table>` from pages | `fire-crab-wire::server` + `fire-crab-ods::catalog` | **the server answers a real query from the database file** - resolves the table name through `RDB$RELATIONS` and counts committed records from the data pages; over the wire, node-firebird's count matches isql exactly on user and system tables |
| Real column projections: `SELECT <cols>` / `SELECT *` from pages | `fire-crab-wire::server` + `fire-crab-ods::catalog` | **the server returns real typed rows** - columns resolved through `RDB$RELATION_FIELDS` (by field id, which reorders vs column position on mixed-width tables), records decoded with the format they name, integers sent as BIGINT and the rest as VARCHAR; node-firebird's result set matches isql value-for-value on user tables, including NULLs and `SELECT *` |
| WHERE-clause filtering: `SELECT ... WHERE <pred>` from pages | `fire-crab-wire::server` | **the server filters rows** - comparisons (`= <> < <= > >=`) on integer and text columns, combined with `AND`/`OR`, plus `IS [NOT] NULL`; evaluated per decoded record, honouring three-valued logic; `SELECT`, `SELECT *` and `COUNT(*)` all take a WHERE, matching isql |
| ORDER BY and aggregates | `fire-crab-wire::server` | **the server sorts and aggregates** - `ORDER BY` on columns or ordinals, ASC/DESC, multi-key, NULLs ordered as the engine does (first ascending); `MIN`/`MAX`/`SUM` over integer columns and `COUNT(col)`/`COUNT(*)`, all composable with WHERE; matches isql value-for-value including NULL results |
| GROUP BY | `fire-crab-wire::server` | **the server groups** - `SELECT <keys and aggregates> ... GROUP BY <cols\|ordinals>`, and multi-aggregate projections with no GROUP BY (one global group, one row even over an empty set); NULL keys bucket together, aggregates computed per group, composable with WHERE and ORDER BY (which sorts the *output*, by name or ordinal); matches isql value-for-value |
| HAVING | `fire-crab-wire::server` | **the server filters groups** - the predicate (comparisons, `AND`/`OR`, `IS [NOT] NULL`) is evaluated on each group's computed output row and may name aggregates *not in the select list* (computed as hidden items) or any grouping column; a global aggregate's single row can be kept or rejected; matches isql, including zero-row results |
| INNER JOIN | `fire-crab-wire::server` | **the server joins two relations** - `FROM t1 [a] [INNER] JOIN t2 [b] ON <col> = <col> [AND ...]` with table-qualified or unambiguous bare columns; NULL and partnerless keys drop out, text keys compare pad-insensitively (CHAR vs VARCHAR), WHERE/ORDER BY see the combined row, `SELECT *` is left columns then right, and a lone `COUNT(*)` counts the joined rows; matches isql value-for-value |
| OUTER JOIN | `fire-crab-wire::server` | **the server answers LEFT/RIGHT/FULL [OUTER] equi-joins** - partnerless preserved-side rows emitted once with the other side NULL-padded (NULL join keys never match, so they surface as padded rows), and WHERE runs on the *padded* row - SQL's join-then-filter order, making `WHERE <right col> IS NULL` the classic anti-join; COUNT(*) over any kind; matches isql value-for-value, the anti-join asserted non-empty so the outer path is provably exercised |
| Native wire types | `fire-crab-wire::server` | **columns travel in the engine's own wire form** - SMALLINT/INTEGER/BIGINT at their own SQL types, NUMERIC/DECIMAL as raw scaled integers with the scale in the describe (the client divides), FLOAT/DOUBLE as IEEE bytes, DATE/TIME/TIMESTAMP as raw day/1e-4-s units, BOOLEAN as an XDR slot; ORDER BY/GROUP BY on them compare numerically; node-firebird's typed decode (numbers, `Date`s, booleans) matches isql |
| INSERT (first DML) | `fire-crab-wire::server` + `fire-crab-ods::dml` | **the server writes real records into the pages** - transaction allocated from the header and committed in the TIP, image laid `rhd_not_packed` into a data-page slot; **the real engine is the oracle**: isql reads the rows back, `gfix -v -full` finds nothing wrong, `gbak` backs the file up; unsupported INSERTs answer SQL errors, never silent no-ops |
| UPDATE / DELETE (version chains) | `fire-crab-wire::server` + `fire-crab-ods::dml` | **the server writes MVCC version chains** - UPDATE copies the old version to a fresh slot flagged `rhd_chain` and rewrites the primary with a back pointer to it; DELETE leaves a header-only `rhd_deleted` stub over the chain (`VIO_modify`/`VIO_erase` + `DPM_update` on the file image); **three engine oracles**: `gfix -v -full` accepts the chains, the same statements applied by the C++ engine to a second copy produce an *identical* table, and `gfix -sweep` - the engine's own GC - collects fire-crab's chains with the version arithmetic exact (46 → 23) and the data intact |
| UPDATE OR INSERT | `fire-crab-wire::server` | **the engine's upsert runs desugared** - try the update whose WHERE is the MATCHING columns (defaulting to the PRIMARY KEY columns read from the catalog, multi-column keys included), store when no row moved - the same plan `UpdateOrInsertNode` compiles; a PK-less table without MATCHING refuses AT PREPARE with the engine's own vector (`isc_primary_key_required`: "Primary key required on table @1", SQLSTATE 22000), not the generic refusal that would hide what to fix; NULL matching values and `?` parameters refuse (the engine matches with null-safe `blr_equiv`; two desugared plans would double-bind one message); differential gate `qa/serve-real-upsert.sh` - the same upsert script through fcwire and the C++ engine leaves two files the engine reads identically |
| System-table projections | `fire-crab-ods::sysfmt` + `fire-crab-wire::server` | **system relations answer like user tables** - their formats (absent from RDB$FORMATS) are *computed* the way ini.epp computes them at database creation, but from the database's own catalog: RDB$RELATION_FIELDS names the fields, RDB$FIELDS types them, the ini.epp offset walk (FLAG_BYTES + MET_align) lays them out. Anchored: the computation must reproduce every offset catalog.rs found by differential testing (4/256/1394/1410, 32/42) - asserted in tests and at every runtime bootstrap. Projections, WHERE, ORDER BY, aggregates, GROUP BY and system-to-system JOINs over RDB$ tables match isql; DML on system relations stays refused |
| BLOB content over the wire | `fire-crab-wire::server` + `fire-crab-ods::format` | **blob columns are first-class** - described SQL_BLOB with sub_type, the 8-byte on-disk bid travels as the quad, and the server answers `op_open_blob(2)`/`op_get_segment`/`op_close_blob` by assembling content from the pages (level 0/1, segment framing stripped per-blob via `rhd_stream_blob`); the first CONTENT differential: node-firebird's assembled text == isql on a 30000-byte multi-page blob (length/head/tail), empty and NULL blobs, and a system blob through the computed system format |
| INT128 + DECFLOAT | `fire-crab-ods::decfloat` + `fire-crab-wire::server` | **decoded, rendered like decNumber, native on the wire** - decimal64/128 DPD decode converted from the engine's embedded decNumber (DPD2BIN table from decDPD.h), rendering per decNumberToString (cohort preserved: 100.00 keeps its zeros; the 0.000001-vs-1E-7 plain/scientific boundary exact); INT128 as a scaled i128. Wire forms exactly as xdr.cpp serializes them. Render differential: `fcstat rows` == isql text on 38-digit INT128 and 34-digit DECFLOAT; wire differential through node-firebird's typed decode; ORDER BY sorts numerically. Three node-firebird client bugs catalogued along the way (negative INT128, huge scale-0 INT128, multi-digit DECFLOAT coefficients - all broken against any server) |
| TIME ZONE types | `fire-crab-ods::tz` + `fire-crab-wire::server` | **decoded and served** - UTC + zone id per ISC_TIME_TZ/ISC_TIMESTAMP_TZ; the 637-entry zone-name table generated from TimeZones.h, offset zones converted to local exactly as TimeZoneUtil (displacement = id - 1439 - ONE_DAY is 24*60-1, an off-by-one the render differential caught first run); named zones render with the right region name but visibly unconverted (their rules need ICU's tzdata - honest placeholder over silently wrong local time); wire forms per xdr.cpp, ORDER BY by UTC instant |
| Write-path page allocation | `fire-crab-ods::dml` | **INSERT grows the table** - pages allocated from the PIP (first free bit, pip_used/pip_min maintained, the FILE extended past EOF), formatted and hooked into the pointer page with fill-bits; back versions grow the relation the same way. 250 inserts grow a table 1 → 8 data pages by gstat's own accounting, `gfix -v -full` accepts the bookkeeping, the engine produces the identical table from the same statements. DML on INDEXED tables refused with a real SQL error until index maintenance is converted - the last big write-path piece |
| Index maintenance (B-tree insertion) | `fire-crab-ods::btw` | **the engine reads through the trees fire-crab writes** - keys byte-exact per btr.cpp compress() (doubles for INTEGER, INT64_KEY for BIGINT, pad-stripped text), btn.h's prefix-compressed node encoding, page splits per split_and_insert incl. root splits; DELETE touches no index and UPDATE adds changed keys, exactly the engine's semantics. 2000 inserts drive leaf+root splits; isql point lookups/range scans/ORDER BY navigate OUR trees (PLAN asserted), `gfix -v -full` cross-checks every record against every entry, duplicate PKs refused. Expression/conditional indexes still refuse DML |
| Multi-segment + descending indexes | `fire-crab-ods::btw` | **compound and DESCENDING keys maintained** - compound keys assembled exactly as BTR_key: per-segment compression interleaved into 5-byte groups (marker byte = segments remaining, STUFF_COUNT data bytes, zero-padded at segment ends); descending keys complement the WHOLE assembled key after assembly (BTR_complement_key), descending NULL = pre-complement 0x00, the 0x01 end-value guard ported. Unique rule corrected BY the differential: only an ALL-NULL key is exempt (btr.cpp:5629) - the live engine refused a partial-NULL duplicate this conversion first assumed exempt. 1500 rows through a compound PK split leaves; the engine point-looks-up, partial-key-scans and navigates DESC through the trees (PLAN asserted), gfix cross-checks every entry |
| Parameter binding | `fire-crab-wire::server` | **prepared statements take real `?` parameters** - the describe's BIND section announces each parameter's target type (what real clients build their encoders from), op_execute's message is decoded from the client's own value-derived BLR (null bitmap + XDR values), and the values bind into INSERT images, UPDATE SET bytes and WHERE comparisons with engine-CVT coercions: integers rescale exactly into NUMERIC, doubles round half-away, a blr_timestamp truncates into DATE/TIME, blr_bool into BOOLEAN. A NULL parameter in a comparison is UNKNOWN; a type-mismatched one raises an SQL error at execute - never a wrong row set. node-firebird drives all of it; the engine applies the literal equivalents and prints the identical table |
| Predicate expression surface | `fire-crab-wire::server` | **LIKE/BETWEEN/IN/NOT/parentheses** - the WHERE grammar is a recursive parser (OR over AND over NOT/parens/leaves) normalized to DNF with NOT pushed into the leaves by De Morgan (sound in three-valued logic); BETWEEN desugars to >= AND <=, IN to OR-of-equalities, their NOT forms fall out - including `x NOT IN (1, NULL)` = UNKNOWN. LIKE matches the STORED value per character with ESCAPE support (CHAR padding counts - differentially confirmed before implementing), parameterised patterns/bounds/lists work through the `?` machinery (a leaf duplicated by the DNF cross-product keeps its one slot). 46 checks, every one the identical SQL through fire-crab and isql on the same file |
| DDL: CREATE TABLE | `fire-crab-ods::ddl` + `fire-crab-wire::server` | **the ENGINE adopts a table fire-crab created from raw bytes** - catalog rows (RDB$FIELDS/RELATION_FIELDS/RELATIONS/FORMATS/PAGES) with every system index maintained, the packed-descriptor format blob and the RDB$RUNTIME field-summary blob (both probe-pinned byte layouts), pointer + index-root pages allocated and registered. The engine then reads the table, INSERTs into it, runs its own CREATE TABLE beside it (id/domain sequences survive), gbak RESTORES it (a restore replays our catalog as real DDL), and DROPs it through our rows - gfix -v -full clean at every step. Three engine laws found by gdb-bisecting a hung attach against the DEBUG engine build: RDB$PAGES rows must be system-transaction (tx 0) records, a primary record clears dpg_secondary + the pointer fill bits, OIT/OAT/OST mirror a clean detach. Constraints/other DDL verbs refuse with real SQL errors |
| DDL breadth: PK/NOT NULL, CREATE INDEX, DROP TABLE | `fire-crab-ods::ddl` | **the ENGINE enforces the constraints fire-crab wrote** - its own duplicate INSERT dies with "violation of PRIMARY or UNIQUE KEY constraint INTEG_n" naming the constraint row fire-crab stored, its point lookups PLAN through the RDB$PRIMARYn and user indexes fire-crab built (CREATE INDEX backfills existing rows; a unique backfill over duplicate data fails like the engine's own index build), NOT NULL enforced both sides (the RSR_field_not_null runtime segment + server-side validation at store). DROP TABLE stubs eight catalog relations' rows, wipes the tx-0 RDB$PAGES rows and releases every owned page - the engine sees the table gone, sweeps the stub chains, gbak restores the file and the restored copy still enforces the PK. Engine law caught live: only pointer/root/TIP/generator rows are legal in RDB$PAGES - a B-tree bucket row CORRUPTs the attach |
| Python firebird-driver compatibility | `fire-crab-wire::server` | **the reference python driver firebird-qa's tests run their SQL through drives fire-crab end-to-end** - a third client kind (after node-firebird's pure JS and C++ isql), the OO API over the real fbclient library. Getting it working exposed protocol details node-firebird was lax about: op_prepare must answer the client's REQUESTED info-item list in order (the fixed-shape buffer made the OO API raise "Unrecognized C++ exception"); op_execute's response object must ECHO the transaction handle (the client nulls its ITransaction on a 0, so commit crashed); string params arrive as blr_text2. SELECT/param-DML/CREATE TABLE+PK/DROP all work, gfix-clean. The full firebird-qa PLUGIN additionally needs the Services API (op_service_attach/info - its bootstrap reads server version/dirs) and op_create - the named next milestones. Also fixed a DROP-with-index page-orphan bug (btr_relation is @28, not @26) |
| Services API + op_create | `fire-crab-wire::server` | **the firebird-qa plugin's session-bootstrap wire operations, driven by the real python driver** - `connect_server` (op_service_attach + op_service_info) answers the server version, home/lock directories, security database and architecture (byte format captured from the real server), which is exactly what the plugin's `pytest_configure` reads; `create_database` (op_create) materialises an empty database with the engine, then serves and mutates it, so the driver runs CREATE TABLE / INSERT / PK-enforced through fire-crab against a fresh file the engine then validates. This does NOT yet run the full suite: past bootstrap the plugin attaches to the `employee` sample database and probes `MON$ATTACHMENTS` and the ODS version to detect the server architecture - monitoring-table and sample-database emulation still ahead |
| **The firebird-qa pytest suite RUNS against fire-crab** | `fire-crab-wire::server` | **the milestone the project aimed at from commit one** - the official firebird-qa runner, through the real python driver, bootstraps fully against fcwire (its banner reads `server: fc [v6.0.0.2076, Embedded, Firebird/Linux/ARM64]`, ODS 14.0), collects the basic functional tests, and **7-9 of them PASS**. This needed op_info_database breadth (DB_ID/ATTACHMENT_ID/ODS/FETCHES - so `con.info.name`/`.id`/`.ods_version` work), a distinct attachment id per connection, MON$ virtual tables reported as empty (the plugin's `MON$ATTACHMENTS` architecture probe returns one all-NULL row → "Embedded"), and the service RUNNING/VERSION info items (so the isql-test fixtures' `set_write_mode` completes). Remaining failures are SQL-surface gaps (e.g. SHOW DATABASE) and version/arch-gated skips, not protocol |
| Select-list expressions | `fire-crab-wire::server` | **arithmetic in the SELECT list** - `SELECT A + B, A * 2, -A, 1 + 1 FROM t`: `+`/`-`/`*`, unary minus and parentheses over scale-0 integer columns and integer literals, plus constant literals, evaluated per decoded row with NULL propagating (SQL three-valued). Parsed with column names, resolved and type-checked at plan time (integer arithmetic → BIGINT, string literal → VARCHAR); un-aliased outputs take the engine's own operation names (ADD/SUBTRACT/MULTIPLY), so the differential compares the isql column header too. Also `op_exec_immediate` (isql's one-shot SET/DDL/DML path, which stopped SHOW VERSION dropping the connection); full SHOW needs the legacy BLR request API (op_compile) |
| SHOW over the BLR request API | `fire-crab-wire::server` | **isql's `SHOW` runs — through the legacy GDML request engine, not DSQL** - `SHOW TABLES/VIEWS/ROLES/FUNCTIONS/PROCEDURES/PACKAGES/FILTERS/PUBLICATIONS` all match the engine byte-for-byte. isql compiles a BLR request (`op_compile`: a blr_begin of message declarations wrapping a blr_for over a system relation that blr_sends each row) and drives it with op_start/op_receive/op_release - a request-execution subsystem separate from DSQL. fire-crab parses the BLR (messages, the for-loop's rse with its boolean and multi-key sort, and the sends with their assignments), walks the named system relation applying the filter and sort, and ships rows as op_send. Two protocol laws the source revealed: nullable output columns come across as blr_parameter2 (value + a separate short null indicator, set to -1 for NULL); and the client requests a whole BATCH per op_receive and reads op_send packets until one carries `p_data_messages = 0`, so the server must ship the entire batch in one burst with only the last packet flagged end (a single-message reply deadlocks). Multi-relation SHOWs (SHOW TABLE <name>, the dependency sub-queries) need joins and blr_matching/blr_gen_id - the next slice |
| Multi-relation SHOW — joined streams | `fire-crab-wire::server` | **`SHOW TABLE <name>`, `SHOW INDICES`, `SHOW DOMAINS`, `SHOW COLLATIONS` match the engine verbatim** - the multi-relation cousins of the single-relation SHOWs. `SHOW INDICES` is `RDB$INDICES CROSS RDB$RELATIONS`; `SHOW TABLE` is a fan of requests (the relation, its columns joined to RDB$FIELDS with their types, PK/FK constraints, indices). The request interpreter gained: **joined streams** (an rse of several (context, relation) streams walked as a nested-loop product, blr_field resolved by context), the verbs those requests use (**blr_equiv** null-safe join equality, **blr_starting**, comparisons, **blr_value_if**, **blr_null**, **blr_matching2** the legacy SLEUTH name filter isql drives with `RDB$+` to hide system names, **blr_gen_id** parsed), the field types they carry (**blr_quad** blob-id, **blr_bool**, **blr_int64**, charset-prefixed *2 text), and request parameters that keep their **text** value. Two protocol fixes: each op_compile gets its **own handle** (a map, not one slot - SHOW INDICES holds the index request open while running a segment request per index), and each op_receive is answered with **one message flagged p_data_messages = 0** (shipping a batch ahead of demand had the inner request read the outer's buffered rows and desync). Generator *values* (real gen pages) are a later slice |
| SHOW GENERATORS — the generator page | `fire-crab-ods` + `fire-crab-wire::server` | **`SHOW GENERATORS`/`SHOW SEQUENCES` and named `SHOW GENERATOR <name>` match the engine verbatim** - name, current value, initial value and increment. The outer request lists the generators (a blr_for over RDB$GENERATORS), then isql reads each current value with a DSQL probe `SELECT GEN_ID(<name>, 0) FROM SYSTEM.RDB$DATABASE`. That value query arrives as **op_exec_immediate2** (75) - the OO client's prepare+execute+return-one-row round-trip - which fire-crab now answers with an **op_sql_response** (78) carrying the scalar row, then a plain op_response. The value itself is read straight from the generator page: id from `RDB$GENERATORS.RDB$GENERATOR_ID`, then slot `id % gensPerPage` of the `pag_ids` page whose `gpg_sequence` is `id / gensPerPage`, a native little-endian SINT64 (`gensPerPage = (page_size − 24) / 8`, ods.cpp:81). `blr_gen_id`, which parsed but read NULL before, now returns real values. A zero step is a read; a non-zero step (an increment - a write) is left to fall through |
| Generator writes | `fire-crab-wire::server` | **`SET GENERATOR <name> TO <n>` and `ALTER SEQUENCE\|GENERATOR <name> RESTART WITH <n>` write the generator page, engine-validated** - the write counterpart of the read above. `SET GENERATOR ... TO n` stores `n`; `RESTART WITH n` stores `n - increment` (the increment read from `RDB$GENERATORS.RDB$GENERATOR_INCREMENT`) so the next `GEN_ID`/`NEXT VALUE FOR` yields `n`. The write goes into the generator's slot on the `pag_ids` page (the same slot the read locates) as a native little-endian SINT64, at op_execute, flushed like DML. `SET GENERATOR` is described as stmt_type set_generator(13), the ALTER form as ddl(5), both with no cursor so the client does not fetch. Oracle: the SAME statements applied by fire-crab and by the engine to two copies leave byte-identical stored values (the engine reads the fire-crab-written file and its own the same), fire-crab reads its own writes back through SHOW GENERATORS, gfix is clean, and a SET on a nonexistent generator raises an error. `GEN_ID(name, step)` with a non-zero step (`NEXT VALUE FOR`) - an increment during a query - is still left to fall through |
| Built-in scalar functions | `fire-crab-wire::server` | **UPPER / LOWER / CHAR_LENGTH / OCTET_LENGTH / SUBSTRING / TRIM / LEFT / RIGHT / REPLACE / POSITION / REVERSE / ABS / MOD / SIGN / LPAD / RPAD, every semantic probed against the engine before it was written** - the parser's string intrinsics (blr_upcase, blr_substring, blr_trim, blr_strlen) and the SysFunction.cpp table, joined to the existing expression surface so they nest with arithmetic, `\|\|`, CAST, conditionals, computed columns and each other. The edges are the content: CHAR_LENGTH counts a CHAR's blank padding, a number under a string function renders first (UPPER(A) = '-7'), SUBSTRING is a clipping window (FROM 0 FOR 3 = 'He') whose literal negative FOR fails at PREPARE exactly as the engine's describe does (isc_bad_substring_length, the value in the vector), TRIM strips whole-string repetitions, MOD rounds scaled operands half away from zero first, POSITION's empty needle answers its start while it fits, LPAD truncates past-length values and cycles its pad. Un-aliased headers carry the engine's names (CHARACTER_LENGTH -> CHAR_LENGTH; an un-aliased IIF headers as CASE - a naming bug this slice caught). The clause splitter learned that SUBSTRING/TRIM carry their own FROM inside parentheses. serve-real-functions.sh: 80 checks, isql running identical SQL against both servers on the same file |
| Function calls in WHERE | `fire-crab-wire::server` | **the predicate surface meets the scalar functions** - `WHERE UPPER(S) = 'X'`, `CHAR_LENGTH(S) > 3`, `MOD(A, 2) = 0`: a call on either side of a comparison, under IS [NOT] NULL, as a LIKE subject, inside BETWEEN/IN, under AND/OR/NOT, in SELECT and DML alike - lexed as one balanced-paren token, evaluated per row through the select-list expression machinery with the predicate surface's three-valued logic and pad-insensitive text compare. The REFUSALS are half the design: a WHERE term answers only true/false and cannot carry the engine's mid-cursor error, so any shape whose per-row evaluation could RAISE (MOD by a non-literal or zero divisor, a length read from a column, a text operand under a numeric function, CAST) refuses at prepare rather than silently excluding a row the engine would have raised on. serve-real-wherefn.sh: 51 checks, isql running identical SQL against both servers |
| CASE expressions | `fire-crab-wire::server` | **searched and simple CASE, and full boolean conditions** - `CASE [operand] WHEN ... THEN ... [ELSE ...] END` on the expression surface, with OR/AND/NOT/parenthesised groups in the conditions (IIF upgraded to the same grammar - the engine parses IIF into a searched CASE, and both header as CASE). The simple form desugars to `=` conditions, carrying the engine's rule for free: `WHEN NULL` never matches. Writing the gate found a STANDING BUG in the older conditionals: a conditional typed from its FIRST branch alone, so `COALESCE(A, 0.5)` announced scale 0 and the scaled branch could not decode - the type now widens to Numeric on any exact-numeric branch (minimum scale, engine-probed: `COALESCE(A, 0.5)` prints -7.0) and every branch value is ALIGNED to the announced scale at emit. serve-real-case.sh: 44 checks. The whole converted expression subsystem - engine sources, probed laws, refusal policy - is documented in [docs/expression-surface.md](docs/expression-surface.md) |
| Temporal expressions | `fire-crab-wire::server` | **EXTRACT, temporal literals, the clock keywords, and DATE/TIME/TIMESTAMP columns as expression operands** - EXTRACT's probed conventions (WEEKDAY 0=Sunday, YEARDAY 0-based, ISO 8601 week, SECOND = NUMERIC(9,4) with its fraction, MILLISECOND = NUMERIC(9,1)), civil-date math round-tripped over leap days and era boundaries, DATE-vs-TIMESTAMP comparison converting as midnight, wrong parts failing at PREPARE like the engine's -105, CURRENT_DATE/LOCALTIME/LOCALTIMESTAMP captured per statement (LOCALTIME truncates its fraction - probed). Temporal values flow through conditionals, concat, WHERE and CASE like any other; mixed temporal kinds in a conditional refuse. serve-real-extract.sh: 42 checks |
| The aggregate surface | `fire-crab-wire::server` | **AVG, MIN/MAX over text/temporal, SUM/AVG over scaled numerics, COUNT(DISTINCT)** - AVG divides truncating toward zero at the operand's scale (probed: AVG(1,2)=1; -2.95/2 = -1.47 not -1.48); SUM/AVG announce the engine's NUMERIC(18,s) widening while MIN/MAX keep the column's own type on the wire; every fold skips NULLs (empty/all-NULL = NULL, COUNT = 0); COUNT(DISTINCT) counts distinct non-NULLs under the predicates' exact equality. Composes with WHERE (function and EXTRACT filters included), GROUP BY NULL-key buckets and HAVING's hidden items. serve-real-aggfn.sh: 36 checks |
| Aggregates over expressions | `fire-crab-wire::server` | **the expression evaluator meets the folds** - SUM(A + ID), AVG(N * 2), MIN(UPPER(S)), COUNT(NULLIF(G, 1)), SUM(IIF(...)), SUM(CASE ... END), MIN(EXTRACT(...)) - evaluated per row before the fold, lone and grouped; an eval error in the argument (SUM(A / 0)) aborts the fetch with the engine's own vector, so the group fold is FALLIBLE by design. SUM widens one step (LONG source → BIGINT, INT64-ranked → INT128, probed on both columns and expressions); AVG keeps its width. Also fixed here: a lone aggregate's column now carries the engine's FUNCTION name instead of CONSTANT (Plan::Scalar carries its header), and GEN_ID of a nonexistent generator refuses instead of answering NULL. serve-real-aggexpr.sh: 37 checks |
| GROUP BY expressions | `fire-crab-wire::server` | **grouping keys computed per row** - GROUP BY UPPER(S) / EXTRACT(YEAR FROM D) / MOD(A, 2) / CASE, and GROUP BY <ordinal> naming an expression item; keys evaluate into synthetic value slots and bucket like fields (NULL keys share one). Select-list expressions match keys STRUCTURALLY (trees compare, not text - `upper( s )` matches `UPPER(S)`); unmatched = the engine's -104, refused. HAVING grew with it: expression aggregates as hidden folds, numeric aggregates through exact scale alignment (AVG(N) > 0), text MIN/MAX pad-trimmed. serve-real-groupexpr.sh: 30 checks |
| Temporal arithmetic | `fire-crab-wire::server` | **DATEADD / DATEDIFF (both syntaxes) and the native operators** - D + 7, 7 + D, TS + 1, DATE - DATE (integer days), TIME - TIME (seconds @ -4), TIMESTAMP differences (nanodays @ -9, truncating to the engine's 9 exact digits). The probed laws: month-end CLAMPING (+1 MONTH on Jan 31 = the leap day), TIME wraps midnight, a DATE absorbs clock units by truncation (+25 HOUR moves a day), DATEDIFF's YEAR/MONTH are calendar-COMPONENT diffs while clock units count BOUNDARY CROSSINGS (signed), MILLISECOND keeps its 0.1-ms digit, and a numeric addend CVT-rounds (D + 0.5 moves a day). DATEDIFF admitted to WHERE; DATEADD refuses there (range errors). serve-real-datemath.sh: 40 checks |
| Fallible predicates + WHERE expressions | `fire-crab-wire::server` | **comparison sides are full expressions, and predicate evaluation can RAISE** - WHERE A + 1 > B, column vs column, (A + B) * 2 = 6, arithmetic BETWEEN/IN bounds, CAST/conditional sides; a per-row eval error (A / 0) propagates through EVERY predicate consumer (row walk, DML targets, CHECK, HAVING, group filters) and reaches the client with the engine's own vector mid-statement - the earlier "no-raise fence" is gone, its shapes admitted. The teeth: a raising row RAISES instead of being silently dropped - exactly the wrong answer a swallowing implementation would give. Error vectors are EXACT: the 22018 carries its offending string (isc_arg_string), and DML WHERE errors answer the engine's own 22012 (ExecErr::Eval channel). serve-real-wherexpr.sh: 35 checks |
| Predicate-surface completion | `fire-crab-wire::server` | **CASE in WHERE, parameters against expressions, join-predicate expressions** - the CASE .. END span lexes by KEYWORD balancing (nested CASEs nest, literals skipped); WHERE UPPER(S) = ? synthesizes its bind target from the expression's type and substitutes the arrived value as a literal at execute; joins take arithmetic/function/CASE/col-vs-col WHERE over the combined row via a synthetic view (ambiguous bare names refuse rather than guess). serve-real-predfull.sh: 20 checks, node-firebird binding the parameter phase |
| SQL → BLR (`src/dsql/`, first slice) | `fire-crab-dsql` | **compiles a view-shaped SELECT to BYTE-IDENTICAL BLR, verified against the engine's own compiler** - the oracle is RDB\$VIEW_BLR: CREATE VIEW makes the engine's DSQL compile the SELECT and store the result verbatim, so every emitted byte is compared against the original compiler's output for the identical statement. Covered: the single-relation RSE, WHERE booleans with the engine's probed compilation laws - NOT folds to inverse comparisons and De-Morgans through AND/OR (blr_not survives only over LIKE/MISSING), NOT BETWEEN expands to lss-OR-gtr, IS NULL is blr_missing, decimal literals keep their written scale, the select list leaves NO trace (the mapping is positional catalog data). Slice 2 added value expressions (a sign folds into numeric literals; blr_negate only before fields), blr_in_list with its u16 count, comma-FROM and INNER JOIN (blr_join nesting like an rse, ON as its boolean), and blr_relation2 aliases (UPPERCASED, DOUBLE-QUOTED - probed). Slice 3 added outer joins (blr_join_type 1/2/3, absent for INNER), left-nested join chains, blr_int64 literals, and the first built-in functions - UPPER/LOWER, CHAR_LENGTH/OCTET_LENGTH (blr_strlen + length-type byte), SUBSTRING (0-based start compiled as an UNFOLDED subtract(from,1) - probed), TRIM (where/spec bytes); an unknown name before `(` refuses, never a field. Slice 4 added CAST (probed dsc layouts: NUMERIC(p<=4) short but DECIMAL(p<=9) always long) and the conditionals: searched CASE/IIF compile as ONE blr_cast over a blr_value_if chain typed by the probed unification law (max int digits + min scale, dtype that fits - long(0) with long(-1) widens to int64), simple CASE is blr_decode and COALESCE blr_coalesce (wrapper-free), NULLIF is cast(value_if(a=b, NULL, a)) typed from its branches only; a field branch under a cast wrapper refuses. Slice 5 added the subquery predicates: EXISTS/SINGULAR (blr_any/blr_unique over one rse, the subquery WHERE its boolean, the select list traceless), IN (SELECT)/ANY/SOME/ALL (blr_ansi_any/_all with a double-nested rse - the outer rse's single stream IS the subquery's rse, the comparison the outer boolean), the probed negation law (quantifier flips, comparison inverts: NOT IN = ansi_all+neq), and scope rules (subquery streams join the context numbering but stay invisible to outer bare names). Slice 6 added DISTINCT (blr_project after the boolean - the one place the select list leaves a trace), scalar subselects (blr_via(blr_singular(rse), value, null), usable anywhere a value is), derived tables (an rse in the stream slot, alias text `"X" "PUBLIC"."T"`, one shared context) and UNION [ALL] (blr_union with per-branch rse + blr_map; the distinct form appends blr_project over blr_fid). Slice 7 opened THE SECOND ORACLE - RDB\$PROCEDURE_BLR: compile_procedure emits a whole FOR SELECT ... DO SUSPEND body byte-identically (message with dsc/null-flag pairs + EOF short, declares, stall, labels, blr_for at CONTEXT 0 - procedures number from 0, views from 1 - ORDER BY as blr_sort, twin sends via blr_parameter2), unlocking ORDER BY. Slice 8 added aggregates + GROUP BY: blr_aggregate as a STREAM with its own context over the source rse, group_by in clause order vs map in select-list order (probed to differ), agg verbs incl. the count-of-values/count-star split, HAVING as the outer boolean over blr_fid slots with probed slot REUSE for equal aggregates, ORDER BY sorting fids. Slice 9 added input parameters: message 0 (dsc/null-flag pairs, no EOF slot), the loop under blr_receive 0, `:name` compiling to blr_parameter2(0, 2i, 2i+1) straight as a value - in comparisons, arithmetic, IN lists, BETWEEN inside aggregate sources. Slice 10 added the singular SELECT INTO (blr_for over blr_singular, no label 1, SUSPEND a sibling send), FIRST/SKIP (rse sub-clauses, probed order stream-FIRST-SKIP-boolean-sort), and the DISTINCT aggregate verbs (COUNT/SUM/AVG dedicated 0x5E-0x60; MIN/MAX(DISTINCT) FOLD to plain - probed). Slice 11 opened THE THIRD ORACLE - RDB\$TRIGGER_BLR: whole trigger bodies byte-identical (leanest wrapper; OLD=ctx 0/NEW=ctx 1 as pseudo-streams; assignments, IF with bare-blr_end missing-ELSE, double-begin blocks; the header leaves no trace). Slice 12 added the DML verbs in trigger bodies: INSERT = blr_store (column list required), DELETE = blr_for over a marks(1,4)-stamped rse + blr_erase, UPDATE = the same loop + blr_modify(org, new) with the new-record context allocated BEFORE the rse stream's - SET targets write new, sources and WHERE read org. Slice 13 added PSQL control: DECLARE with initialisers (triggers group declares-then-inits, procedures interleave - both probed), blr_variable reads/assignments, WHILE (label + blr_loop + if/leave, encounter-ordered labels), INSERTING/UPDATING/DELETING (eql(internal_info(6), 1/2/3), composing under NOT/AND/OR). Slice 14 unified the surfaces: procedure bodies run the same statement machine as triggers - outputs ARE variables, SUSPEND is a statement, RETURNS optional (one-slot EOF message), bare names resolve vars-then-inputs outside stream scopes but stay columns inside, bodies mix FOR SELECT/SELECT INTO/DML/IF/WHILE/SUSPEND freely. Slice 15 added the call surface: EXECUTE PROCEDURE (blr_exec_proc: counted name, in-count+values, out-count+variables), EXCEPTION (blr_abort+2+name), EXIT (blr_leave 0 - the wrapper's label), (FOR) SELECT in trigger bodies, and the aggregate-context law generalised to stream+1 anywhere (probed at three contexts - refusal flip nine). Slice 16 opened THE FOURTH ORACLE - column BLR: DEFAULT clauses (RDB\$DEFAULT_VALUE) and COMPUTED BY (RDB\$COMPUTED_BLR), both blr_version5-value-blr_eoc; defaults as narrow as the engine's grammar, computed columns taking the whole expression surface at an anonymous ctx 0. Slice 17 added domain defaults (RDB\$FIELDS), expression indexes (RDB\$EXPRESSION_BLR - the computed frame again) and CHECK constraints (the engine's own system triggers: if over the NEGATED condition + abort gds_code 'check_constraint', fields at ctx 1) - and FIXED a latent gap the CHECK battery exposed: non-integer IN-list items are CAST to the left side's catalog type, so they refuse now. Slice 18 added the seventh store - domain validation (RDB\$VALIDATION_BLR: the RAW un-negated boolean, VALUE = blr_fid(0,0)) - plus GEN_ID vs NEXT VALUE FOR (blr_gen_id/blr_gen_id2 - two verbs chosen by syntax) and POST_EVENT (blr_post). Slice 19 added error handling: WHEN-carrying blocks = blr_block (guarded begin, error_handler + ANY/EXCEPTION/GDSCODE codes, handler statement) while plain blocks stay double-begins; ROW_COUNT = internal_info(5). Slice 20 converted UPDATE OR INSERT (modify-loop with blr_equiv MATCHING + row_count-guarded store; contexts store/new/org in order), CURRENT_CONNECTION/CURRENT_TRANSACTION (internal_info 1/2), multiple handlers and handler-body blocks (flips ten and eleven). Slice 21 added autonomous transactions (blr_auto_trans), multi-column MATCHING, WHEN SQLCODE (flip twelve), and CURSORS (blr_dcl_cursor with the name riding the relation2 alias, derived_expr outputs, cursor_stmt open/close/fetch) - plus the pinned init-defers-past-cursors ordering law. Slice 22 COMPLETED the cursor surface: WHEN SQLSTATE (code 8 + counted string - flip thirteen), handlers carrying their OWN handlers (nested blr_block WITH its section - flip fourteen), cursor table aliases (`"CX" "A"` replacing the schema-qualified table in the relation2 alias) with qualified-column resolution, AGGREGATE cursors (blr_aggregate at ctx+1 claiming a SECOND slot, outputs as BARE blr_fid - no derived_expr), and positioned DML: DELETE/UPDATE ... WHERE CURRENT OF = blr_erase at the cursor's own context / blr_modify to one fresh slot, both stamped marks(1,1) TRAILING where loop marks LEAD. Slice 23 added MERGE - one probed sentence: for(marks(1,6), rse(join2(source@0, target@1, [LEFT], ON), [branch-union boolean]), if(missing(dbkey(target)), store, modify/erase+marks(1,2))) - the join LEFT only when NOT MATCHED needs unmatched rows, the rse boolean ORing branch conditions in canonical order (SQL branch order traceless), branch contexts allocating update-new then store - plus FOR SELECT ... AS CURSOR (the labeled FOR loop with the cursor name riding the relation2 alias, INTO optional and derived_expr-wrapped, WHERE CURRENT OF at the FOR's context) and cursors in TRIGGER bodies (declarations keep their source slot among the grouped declares, numbering past OLD/NEW). Slice 24 added conditional MERGE branches (WHEN [NOT] MATCHED AND joins the rse boolean's branch term and nests if(cond, action, bare end) in its slot), SCROLL cursors + FETCH directions (blr_scrollable; cursor_stmt sub-verb 3 + direction byte + offset value, blr_null unless ABSOLUTE/RELATIVE), DML RETURNING INTO - THREE shapes for one clause: INSERT = blr_store2 + second begin, UPDATE = blr_modify2 under a SINGULAR rse reading the NEW record, DELETE = a begin(returning, PLAIN erase) - and EXECUTE STATEMENT (blr_exec_sql; [FOR] INTO = blr_exec_into with flag 1 singleton / flag 0 + DO statement, variables LAST). Slice 25 added multi-branch MERGE (branches of one kind chain if(cond, action, <next>) in SQL order - the next if fills the else BY POSITION, an unconditional LAST branch fills it directly; the rse boolean ORs kind-terms of left-nested or-chains, each simplified to its bare missing-test when the kind has an unconditional branch; contexts allocate BY KIND in branch order), parameterized EXECUTE STATEMENT (('sql') (vals) = the FULL blr_exec_stmt: tags 1 in-count / 2 out-count / 3 sql / 4 the FOR form's DO statement / 11 input values / 13 output variables / blr_end - literal no-param forms keep the compact verbs) and WITH LOCK (blr_writelock between the stream and the boolean, in FOR SELECT, DECLAREd cursors and AS CURSOR loops alike). Slice 26 added SUBROUTINES - DECLARE FUNCTION/PROCEDURE = blr_subfunc_decl/blr_subproc_decl (counted name, type, flag byte - deterministic vs selectable - u16-counted param names with the function's return slot UNNAMED, the WHOLE inner body's BLR as a u32-counted blob compiled by the same machinery on a fresh parser); RETURN = begin(assign slot 0, the no-EOF send, leave 0); calls = blr_invoke_function/_procedure with the sub id clause; a subroutine's INPUTS reserve variable slots and a void one drops blr_stall - plus named EXECUTE STATEMENT parameters (tag 12, counted name + value) and the ON EXTERNAL/AS USER/PASSWORD/ROLE modifiers (tags 5/6/7/14, any of which forces the full form). AND A LATENT FIX: procedure LOCALS group declare-declare-init-init (outputs interleave) - sixteen slices read single-local bodies as interleaving, and no battery statement had two inited locals until this slice's probe. Slice 27 turned the converted surface loose INSIDE subroutines: blr_relation3 (counted schema PUBLIC, counted EMPTY package, the name, the alias slot always present - relation2's alias string or a counted empty) replaces relation/relation2 in subroutine bodies via one emission switch - INSERT, DML loops, SELECT INTO, FOR SELECT, cursors, AS CURSOR positioned DML, UPDATE OR INSERT and MERGE all run inside subs in the battery; zero-arg sub-function calls compile (the argument tag rides with count 0). Slice 28 flipped one of the OLDEST refusals - ALIASED streams in body statements (FOR SELECT/DELETE/UPDATE with table aliases: relation2 + quoted alias at top level, relation3's always-present alias slot in subroutines, qualified refs through the unchanged field machinery) - and let SUBROUTINES into TRIGGER bodies (the same grouped-declare slot cursors take; a probe passed NEW.ID as a sub-function argument). Slice 29 dropped the second slice-7 refusal in two slices - SUBQUERIES IN BODY STATEMENTS (EXISTS/SINGULAR, IN (SELECT)/ANY/ALL, scalar subselects in WHEREs and assignments; the view machinery carried over on two changes: subquery ctx = next id at the statement's numbering, and a host slot keeping the enclosing statement's stream visible to qualified names) - and aliased AS CURSOR (the cursor name pairs with the ALIAS: \"CU\" \"E\"). Slice 30 added AGGREGATE scalar subselects - via(singular, rse1(aggregate at the NEXT slot over the inner rse with its WHERE inside, zero group keys, a one-slot map), fid, null) - in assignments, WHERE comparisons, SET values and subroutine bodies; subqueries inside a CURSOR's rse stay guarded (their streams INHERIT the cursor alias string - probed). Slice 31 converted that infection - a post-parse STAMP walks the cursor's boolean tree pairing the cursor name with each subquery stream's alias or schema-qualified name, relation3 taking the same string inside subroutines - plus DISTINCT aggregate scalars (dedicated verbs, MIN/MAX folding). Slice 32 added JOINS in body FOR SELECTs (INNER/LEFT/RIGHT/FULL chains in FOR SELECT and singular SELECT INTO - the view's left-nested chain at body numbering; the MERGE scope generalized to a stream-index RANGE, bare names refusing - the gate caught a bare column binding to the FIRST stream where the engine uses the catalog) and PACKAGED calls (EXECUTE PROCEDURE PKG.P = blr_exec_proc2, PKG.F(x) = blr_function2 with a count BYTE - compact verbs, not the subroutine invoke forms). Slice 33 completed the join story: AGGREGATES over joins (the chain inside the aggregate's inner rse, the aggregate claiming the slot after ALL join streams) and JOINED CURSORS (both streams carrying the cursor pairing via the slice-31 stamp; each output's derived_expr wrap naming its column's OWN stream; positioned DML on joined cursors refusing). Slice 34 added WINDOW FUNCTIONS: blr_window wrapping the inner rse (WHERE inside), per-window blr_partition_by (context, partition keys as source fields then REMAPPED map-fids, sort, map); passthrough columns share the DEFAULT window with empty-spec OVER () functions, each distinct spec gets its own window in ENCOUNTER order; ROW_NUMBER/RANK/DENSE_RANK = blr_agg_function (counted name + argc). Slice 35 completed the window story: LAG/LEAD/FIRST_VALUE/LAST_VALUE (LAG/LEAD canonicalize to THREE args - value, offset filled 1, default filled NULL) and FRAME extents (the v4 blr_window_win: subcoded partition/order/map, extent unit RANGE 0/ROWS 1, bounds with optional values, its OWN end; UNBOUNDED = bound sans value, single-bound implies CURRENT ROW; v3/v4 windows mix freely). Slice 36 added NTH_VALUE (a FROM FIRST indicator appended - literal 0) and DERIVED TABLES in body FOR SELECTs (the view convention at body numbering: inner rse in the stream slot, inner WHERE inside, outer at the rse level, one shared context; pass-through lists only) - and PINNED the subquery-derived shape that had been live through composition without a covering test. Slice 37 added DERIVED-COLUMN ALIASES: (SELECT UID AS X) D records an outer-to-inner map and every outer reference to X translates to the inner column at the shared context - one translation point in the field resolver; UNION in bodies probed (blr_union at the first slot) and left a named refusal with its transcript. Slice 38 cashed it - UNION [ALL] in body FOR SELECTs (the union claims the FIRST slot via a lookahead reservation, per-branch rses and maps, DISTINCT appending blr_project) - and flipped a refusal from slice 12: INSERT..SELECT (a marks-stamped FOR loop over the source rse, one store per row, the source stream numbering first), in procedures, subroutines and triggers. Slice 39 added WITH ctes - the engine INLINES them as derived tables (the cte name becomes the alias, column aliases translate); the parser records the body's token span and expands at the FROM reference - and expression INSERT..SELECT (full value expressions at the source stream), the FIFTIETH refusal to fall. GROUP BY expressions probed (items rebuilt over mapped fids) and left named. Slice 40 cashed it and more: select-item EXPRESSIONS (full value surface at the stream ctx), GROUP BY EXPRESSIONS (raw expr in the group list, BARE fields in the map, items and aggregate ORDER keys REBUILT over fids - the slice-8 map law generalized) and MULTIPLE ctes (comma lists, each expanding once, one pinned inside a correlated subquery). Slice 41 opened five doors: expression sort keys (bare literals refusing - the engine reads them as POSITIONS), window passthrough expressions (the group-expr law in window clothing), OFFSET/FETCH (the standard spelling of SKIP/FIRST), PLAN (tbl NATURAL) (blr_plan/retrieve/stream-again/sequential, last in the rse) and parameters in HAVING - a slice-9 refusal felled by the gate's own battery. Slice 42 added DISTINCT in body FOR SELECTs (blr_project over the select columns after the boolean - the slice-6 view law at body numbering unchanged), PLAN (tbl INDEX (names)) (blr_indices + counted index names standing where blr_sequential stood) and the legacy ROWS m TO n limits desugaring to UNFOLDED arithmetic (first = add(subtract(n, m), 1), skip = subtract(m, 1) - literal trees in the OFFSET/FETCH rse slots); recursive CTEs probed - blr_recurse, a union-like whose RECURSIVE branch has zero streams, reading the recursion's context by fid - and left a named refusal with the transcript. qa/dsql-view-blr.sh: 128 + qa/dsql-proc-blr.sh: 253 + qa/dsql-trig-blr.sh: 48 + qa/dsql-field-blr.sh: 39 checks |
| Everything else | — | see [docs/subsystem-map.md](docs/subsystem-map.md), [CHANGELOG.md](CHANGELOG.md) for the increment-by-increment history, and [docs/porting-playbook.md](docs/porting-playbook.md) for the method and core algorithms in language-agnostic pseudocode |

**On the firebird-qa milestone, precisely.** firebird-qa drives a *server*,
so the suite only becomes applicable once fire-crab can *accept* connections,
not just make them. Both halves now exist:

- **Client** (`fire-crab-wire`): connects to the running C++ engine; every
  query is checked against isql. This validated the wire codec (XDR, SRP-256,
  the message/BLR formats) end-to-end against the real server first.
- **Server** (`fire-crab-wire::server`): accepts TCP connections and speaks
  the same protocol the C++ `src/remote/` server does. **node-firebird - an
  independent, third-party client library with no fire-crab code in it -
  negotiates protocol 20, authenticates via the server half of SRP-256
  (no password on the wire), arms Arc4 wire encryption with the derived
  session key, attaches, and drives the full statement pipeline
  (transaction → allocate → prepare → execute → fetch), decoding the
  returned value correctly.** The C++ `isql` client authenticates via
  SRP-256 and attaches as well, then drives its richer post-attach ops
  (op_cancel, op_info_database) until it reaches op_exec_immediate.

The server now answers **real queries** from the database file the client
attaches to, dispatching into the converted `ods` engine rather than returning
a constant:

- `SELECT COUNT(*) FROM <table>` resolves the table through `RDB$RELATIONS`
  (read straight from its data pages by `fire-crab-ods::catalog`) and counts
  the committed records - matching isql on every user and system table tested.
- `SELECT <cols> FROM <table>` and `SELECT *` return **real typed rows**:
  columns resolved through `RDB$RELATION_FIELDS`, each record decoded with the
  format it names, integers sent as BIGINT and the rest rendered as VARCHAR.
  Over the encrypted wire, node-firebird's result set matches isql
  value-for-value on user tables - including NULLs, mixed-width tables (where
  a column's field id is *not* its position), and `SELECT *`.
- A **WHERE clause** filters those rows: comparisons (`= <> < <= > >=`) on
  integer and text columns, combined with `AND`/`OR` (and binding tighter, so
  the predicate is OR-of-ANDs) plus `IS [NOT] NULL`, evaluated per decoded
  record with SQL three-valued logic (a comparison against NULL is UNKNOWN,
  the row excluded). `SELECT`, `SELECT *` and `COUNT(*)` all take a WHERE, and
  a predicate the parser does not fully support makes the query fall back to
  the fixed value rather than answer it without the filter.
- **ORDER BY** sorts the result: by column name or 1-based ordinal, `ASC`/
  `DESC`, multiple keys, with NULLs ordered as the engine does (first when
  ascending, last when descending). Matching rows are collected and sorted
  before they are sent.
- **Aggregates** `MIN`/`MAX`/`SUM` over an integer column and
  `COUNT(col)`/`COUNT(*)` return a single value (NULL when the set is empty),
  composable with WHERE - matching isql, including that `SUM`/`MIN`/`MAX`
  ignore NULLs and `COUNT(col)` counts only non-NULLs.
- **GROUP BY** buckets the filtered rows by the key columns (named or by
  select-list ordinal) and computes each aggregate per bucket; NULL keys form
  one group, as SQL requires. Every bare select-list column must be a group
  key (anything else falls back rather than answering wrong). A projection of
  several aggregates with *no* GROUP BY is one global group - exactly one
  output row, even over an empty set (`COUNT` 0, the rest NULL). ORDER BY on
  a grouped query sorts the output rows, by output column name or ordinal
  (so `ORDER BY 2 DESC` on a `COUNT(*)` column works).
- **HAVING** filters the groups after aggregation, where WHERE filtered the
  rows before it: the predicate is evaluated on each group's computed output
  row, and may reference aggregates that are *not* in the select list
  (`SELECT DEPT_ID ... HAVING SUM(SALARY) > 190000` computes the sum as a
  hidden item) or any grouping column (`HAVING DEPT_ID IS NULL` selects the
  NULL-key bucket). With no GROUP BY it keeps or rejects the single global
  row - so a HAVING can legitimately answer zero rows. An aggregate in a
  WHERE clause, or a non-grouped column in a HAVING, is invalid SQL and
  falls back.
- **INNER JOIN** matches two relations' decoded records on one or more
  AND-ed `ON` column equalities: each joined row is the left record's values
  followed by the right's, so projections, WHERE and ORDER BY all address
  the combined row - through table-qualified names (`E.ID`, or `EMP.ID`
  when no alias hides the table) or bare names when exactly one side has
  the column. NULL keys never join (`=` with NULL is UNKNOWN), keys with no
  partner drop out, text keys compare with trailing blanks insignificant
  (a `CHAR(10)` key joins the same `VARCHAR` text), and duplicate keys
  yield their full cross product. `SELECT *` is all left columns then all
  right; a lone `SELECT COUNT(*)` counts the joined rows. Cross
  joins, chained joins, comma-list FROMs and grouping over a join fall
  back.
- **Outer joins.** `LEFT`, `RIGHT` and `FULL [OUTER] JOIN`: the
  preserved side's partnerless rows are emitted once with the other
  side all NULLs (FULL preserves both, appending the right rows nothing
  matched). The WHERE filter runs on the padded row - SQL evaluates the
  join first, the filter after - so `WHERE <right col> IS NULL` on a
  LEFT join selects exactly the rows that found no partner, the classic
  anti-join, and rows whose join key is NULL (which never matches
  anything) come back NULL-padded instead of vanishing.
- **Native wire types.** Every column type the record decoder handles
  exactly is described and encoded as the engine would: integers at their
  own width (`SQL_SHORT`/`SQL_LONG`/`SQL_INT64`), `NUMERIC`/`DECIMAL` as
  the raw stored integer plus the scale in the describe - the client
  divides, which is the engine's contract - `FLOAT`/`DOUBLE` as IEEE
  bytes, `DATE`/`TIME`/`TIMESTAMP` as raw Modified-Julian-day and
  1/10000-second units, `BOOLEAN` as an XDR int slot. Sorting and
  grouping on these compare numerically (9.50 before 12.30; dates
  chronologically). node-firebird decodes them through its normal typed
  path - JS numbers, `Date` objects, booleans - and matches isql.
- **INSERT writes the pages.** `INSERT INTO <t> [(cols)] VALUES (...)`
  (single row, literal values) allocates a transaction from
  `hdr_next_transaction`, marks it committed in the transaction
  inventory, builds the record image (null flags + fields at their
  descriptor offsets, length pinned to a live record's `fmt_length`) and
  lays it `rhd_not_packed` into a data-page slot - dpm.epp's directory
  arithmetic in reverse. The oracle is the engine itself: isql SELECTs
  the rows fire-crab wrote, `gfix -v -full` validates the whole file
  (a check with teeth - one corrupted slot makes it throw), `gbak`
  backs it up. It is an *offline-style* writer (whole-file flush, no
  careful-write ordering, no page allocation or index maintenance yet),
  and an INSERT the server cannot honour raises a real SQL error over
  the wire - for DML, a silent fallback would mean a client believing
  its write succeeded.
- **UPDATE/DELETE write version chains.** `UPDATE <t> SET col = lit
  [, ...] [WHERE ...]` and `DELETE FROM <t> [WHERE ...]` (the WHERE is
  the same grammar SELECT filters with) do what `VIO_modify`/`VIO_erase`
  do on the page image: the current version is *copied* to a fresh slot
  and flagged `rhd_chain`, then the primary slot is rewritten under a
  freshly committed transaction - the new image for an update, a
  header-only `rhd_deleted` stub for a delete - with
  `rhd_b_page`/`rhd_b_line` pointing at the copy. Back versions are full
  images (legal - the engine writes full back versions itself when a
  delta would not shrink), repeated updates extend the chain, and each
  statement executes against a working copy of the file so a failure
  leaves no half-written chains. The oracle is the engine three ways:
  `gfix -v -full` accepts the chains; the same statements applied by the
  C++ engine to a second copy of the same clean database produce an
  identical final table; and `gfix -sweep` - the engine's own garbage
  collector - consumes the chains fire-crab wrote, the version count
  dropping exactly as arithmetic predicts (30 rows, 9 statements → 46
  versions, sweep → 23) with the data unchanged.

The subtlety that had to be right: on a table mixing column widths the engine
lays fields out physically by alignment, so `RDB$FIELD_ID` (the record-format
index) diverges from `RDB$FIELD_POSITION` (the declared order); projecting by
position instead of field id silently returns the wrong column, which a
uniform-width table never reveals.

Projections, WHERE, joins, GROUP BY, HAVING, ORDER BY and aggregates
cover user tables AND system relations - the latter's formats (absent
from `RDB$FORMATS`; the engine builds them at creation from compiled-in
tables, ini.epp:1053-1088) are computed by the same offset walk from the
database's own catalog rows, so the file describes itself; only the two
catalog-reading relations are compiled into `sysfmt`, and the computation
must reproduce the offsets `catalog.rs` established empirically before it
is trusted. Integers, scaled numerics, float/double, date/time/timestamp,
boolean, text, `INT128`, `DECFLOAT` and the TIME ZONE types all travel
in native wire form, and blob columns are served as real blobs - the id
in the row, the content through the blob ops; named-zone local times
render visibly unconverted (their rules need tzdata), and other shapes
fall back to the fixed value.
Widening further (index maintenance - the last big write-path piece -
and scaled/temporal SET values) is the
work that continues - but the fixed answer is no longer fixed, the server
writes records and version chains the real engine validates and
garbage-collects, and the protocol server it all runs on is proven
against a genuine client.

Current QA state: `fcstat header` output is **byte-identical on the compared
fields with `gstat -h` across 123 real Firebird 6 databases** (every scratch
database generated by the paper's hands-on samples), and the record walk now decodes **full rows from raw pages** — RDB$FORMATS
bootstrap (the system format hardcoded, exactly like the engine's own
bootstrap), blob-id resolution through the record-number formulas, segmented
blob assembly, descriptor-driven field decode — **matching live
`SELECT` output value-for-value** on every compared column of every table
tested, from 0 to 200,000 rows. See
[docs/qa-and-benchmarks.md](docs/qa-and-benchmarks.md) for the numbers and the
honest caveats attached to them.

## Quick start

```sh
cargo test                                   # layout + round-trip tests
cargo build --release
./target/release/fcstat header  /path/to/db.fdb
./target/release/fcstat census  /path/to/db.fdb
./target/release/fcstat tip     /path/to/db.fdb

# differential QA against the C++ engine's gstat:
GSTAT=/opt/firebird/bin/gstat qa/diff-gstat.sh /path/to/*.fdb

# C++-vs-Rust timing on the same file:
GSTAT=/opt/firebird/bin/gstat bench/compare.sh /path/to/db.fdb
```

## Layout

- `crates/ods` — the conversion itself, one module per converted C++ unit,
  each headed by a comment naming its source (`ods.h`, `sqz.cpp`, `tra.h`).
- `crates/dsql` — SQL → BLR (`src/dsql/`), differentially tested against
  the engine's own compiler through `RDB$VIEW_BLR`.
- `crates/fcstat` — the inspector tool that makes the conversion observable.
- `qa/` — differential testing against the C++ engine (and the plan for
  adopting the [firebird-qa](https://github.com/FirebirdSQL/firebird-qa)
  suite once a wire-protocol milestone makes it applicable).
- `bench/` — C++-vs-Rust measurements with their caveats attached.
- `docs/` — methodology, subsystem map, QA strategy.
- `CHANGELOG.md` — the increments grouped by day and theme, newest first.
- `docs/porting-playbook.md` — the method and core algorithms in
  pseudocode, for the next agent porting to another language.

## License

The conversion follows the code it converts: Initial Developer's Public
License (IDPL), compatible with Firebird's IPL/IDPL licensing.
