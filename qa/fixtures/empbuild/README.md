The employee sample's build scripts, `empddl.sql` and `empdml.sql`, as
shipped in Firebird 5.0.4 `examples/empbuild/` (Initial Developer's
Public License, see the file headers). `qa/serve-real-empbuild.sh` runs
them through fire-crab and through the engine and holds the two
databases against each other.
